<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$options = array(

    'contact' => array(
        'type'  => 'textarea',
        'label' => __( 'Contact Shortcode', 'fw' ),
        'desc'  => __( 'Insert here contact shortcode (contact form 7 used)', 'fw' ),
    )
);	