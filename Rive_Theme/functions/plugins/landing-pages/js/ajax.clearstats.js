 jQuery(document).ready(function($) { 					jQuery('.clear_stats').live('click', function(){		var this_id = this.id.replace('lp_clear_','');				var data = {			action: 'lp_clear_stats',					lp_id: this_id				};		if (confirm('Are you sure you want to delete the stats for this landing page?')) {					jQuery.post(ajaxadmin.reseturl, data, function(response) {				if (response)				{										setTimeout(function(){                        location.reload();	                    }, 500);   													}			});						}	});								});