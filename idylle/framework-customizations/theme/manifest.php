<?php if ( ! defined( 'FW' ) ) {
    die( 'Forbidden' );
}

$manifest = array();

$manifest['id'] = 'idylle';

$manifest['supported_extensions'] = array(
    'page-builder' => array(),
    'breadcrumbs' => array(),
    'backup' => array(),
);
