<?php

/**
 * Settings
 */
$ishyoboy_shortcodes = Array(
    'grid12' => 'ishyoboy_shortcode_grid12',
    'grid12_nested1' => 'ishyoboy_shortcode_grid12',
    'grid12_nested2' => 'ishyoboy_shortcode_grid12',
    'grid12_nested3' => 'ishyoboy_shortcode_grid12',

    'grid11' => 'ishyoboy_shortcode_grid11',
    'grid11_nested1' => 'ishyoboy_shortcode_grid11',
    'grid11_nested2' => 'ishyoboy_shortcode_grid11',
    'grid11_nested3' => 'ishyoboy_shortcode_grid11',

    'grid10' => 'ishyoboy_shortcode_grid10',
    'grid10_nested1' => 'ishyoboy_shortcode_grid10',
    'grid10_nested2' => 'ishyoboy_shortcode_grid10',
    'grid10_nested3' => 'ishyoboy_shortcode_grid10',

    'grid9' => 'ishyoboy_shortcode_grid9',
    'grid9_nested1' => 'ishyoboy_shortcode_grid9',
    'grid9_nested2' => 'ishyoboy_shortcode_grid9',
    'grid9_nested3' => 'ishyoboy_shortcode_grid9',

    'grid8' => 'ishyoboy_shortcode_grid8',
    'grid8_nested1' => 'ishyoboy_shortcode_grid8',
    'grid8_nested2' => 'ishyoboy_shortcode_grid8',
    'grid8_nested3' => 'ishyoboy_shortcode_grid8',

    'grid7' => 'ishyoboy_shortcode_grid7',
    'grid7_nested1' => 'ishyoboy_shortcode_grid7',
    'grid7_nested2' => 'ishyoboy_shortcode_grid7',
    'grid7_nested3' => 'ishyoboy_shortcode_grid7',

    'grid6' => 'ishyoboy_shortcode_grid6',
    'grid6_nested1' => 'ishyoboy_shortcode_grid6',
    'grid6_nested2' => 'ishyoboy_shortcode_grid6',
    'grid6_nested3' => 'ishyoboy_shortcode_grid6',

    'grid5' => 'ishyoboy_shortcode_grid5',
    'grid5_nested1' => 'ishyoboy_shortcode_grid5',
    'grid5_nested2' => 'ishyoboy_shortcode_grid5',
    'grid5_nested3' => 'ishyoboy_shortcode_grid5',

    'grid4' => 'ishyoboy_shortcode_grid4',
    'grid4_nested1' => 'ishyoboy_shortcode_grid4',
    'grid4_nested2' => 'ishyoboy_shortcode_grid4',
    'grid4_nested3' => 'ishyoboy_shortcode_grid4',

    'grid3' => 'ishyoboy_shortcode_grid3',
    'grid3_nested1' => 'ishyoboy_shortcode_grid3',
    'grid3_nested2' => 'ishyoboy_shortcode_grid3',
    'grid3_nested3' => 'ishyoboy_shortcode_grid3',

    'grid2' => 'ishyoboy_shortcode_grid2',
    'grid2_nested1' => 'ishyoboy_shortcode_grid2',
    'grid2_nested2' => 'ishyoboy_shortcode_grid2',
    'grid2_nested3' => 'ishyoboy_shortcode_grid2',

    'grid1' => 'ishyoboy_shortcode_grid1',
    'grid1_nested1' => 'ishyoboy_shortcode_grid1',
    'grid1_nested2' => 'ishyoboy_shortcode_grid1',
    'grid1_nested3' => 'ishyoboy_shortcode_grid1',

    // Columns alternatives
    'one_full' => 'ishyoboy_shortcode_grid12',
    'one_half' => 'ishyoboy_shortcode_grid6',
    'one_third' => 'ishyoboy_shortcode_grid4',
    'one_fourth' => 'ishyoboy_shortcode_grid3',
    'one_sixth' => 'ishyoboy_shortcode_grid2',
    'two_thirds' => 'ishyoboy_shortcode_grid8',
    'three_fourths' => 'ishyoboy_shortcode_grid9',

    'one_full_nested1' => 'ishyoboy_shortcode_grid12',
    'one_half_nested1' => 'ishyoboy_shortcode_grid6',
    'one_third_nested1' => 'ishyoboy_shortcode_grid4',
    'one_fourth_nested1' => 'ishyoboy_shortcode_grid3',
    'one_sixth_nested1' => 'ishyoboy_shortcode_grid2',
    'two_thirds_nested1' => 'ishyoboy_shortcode_grid8',
    'three_fourths_nested1' => 'ishyoboy_shortcode_grid9',

    'one_full_nested2' => 'ishyoboy_shortcode_grid12',
    'one_half_nested2' => 'ishyoboy_shortcode_grid6',
    'one_third_nested2' => 'ishyoboy_shortcode_grid4',
    'one_fourth_nested2' => 'ishyoboy_shortcode_grid3',
    'one_sixth_nested2' => 'ishyoboy_shortcode_grid2',
    'two_thirds_nested2' => 'ishyoboy_shortcode_grid8',
    'three_fourths_nested2' => 'ishyoboy_shortcode_grid9',

    'one_full_nested3' => 'ishyoboy_shortcode_grid12',
    'one_half_nested3' => 'ishyoboy_shortcode_grid6',
    'one_third_nested3' => 'ishyoboy_shortcode_grid4',
    'one_fourth_nested3' => 'ishyoboy_shortcode_grid3',
    'one_sixth_nested3' => 'ishyoboy_shortcode_grid2',
    'two_thirds_nested3' => 'ishyoboy_shortcode_grid8',
    'three_fourths_nested3' => 'ishyoboy_shortcode_grid9',

    'box' => 'ishyoboy_shortcode_box',
    'box_nested1' => 'ishyoboy_shortcode_box',
    'box_nested2' => 'ishyoboy_shortcode_box',
    'box_nested3' => 'ishyoboy_shortcode_box',
    'iconic_box' => 'ishyoboy_shortcode_box',
    'iconic_box_nested1' => 'ishyoboy_shortcode_box',
    'iconic_box_nested2' => 'ishyoboy_shortcode_box',
    'iconic_box_nested3' => 'ishyoboy_shortcode_box',

    'alert' => 'ishyoboy_shortcode_alert',
    'alert_nested1' => 'ishyoboy_shortcode_alert',
    'alert_nested2' => 'ishyoboy_shortcode_alert',
    'alert_nested3' => 'ishyoboy_shortcode_alert',

    'box_group' => 'ishyoboy_shortcode_box_group',
    'box_group_nested1' => 'ishyoboy_shortcode_box_group',
    'box_group_nested2' => 'ishyoboy_shortcode_box_group',
    'box_group_nested3' => 'ishyoboy_shortcode_box_group',

    'row' => 'ishyoboy_shortcode_row',
    'row_nested1' => 'ishyoboy_shortcode_row',
    'row_nested2' => 'ishyoboy_shortcode_row',
    'row_nested3' => 'ishyoboy_shortcode_row',

    'button' => 'ishyoboy_shortcode_button',

    'icon' => 'ishyoboy_shortcode_icon',
    'icon_nested1' => 'ishyoboy_shortcode_icon',
    'icon_nested2' => 'ishyoboy_shortcode_icon',
    'icon_nested3' => 'ishyoboy_shortcode_icon',

    'social' => 'ishyoboy_shortcode_social',
    'social_nested1' => 'ishyoboy_shortcode_social',
    'social_nested2' => 'ishyoboy_shortcode_social',
    'social_nested3' => 'ishyoboy_shortcode_social',

    'social_share' => 'ishyoboy_shortcode_social_share',

    'headline' => 'ishyoboy_shortcode_headline',
    'headline_nested1' => 'ishyoboy_shortcode_headline',
    'headline_nested2' => 'ishyoboy_shortcode_headline',
    'headline_nested3' => 'ishyoboy_shortcode_headline',

    'section_headline' => 'ishyoboy_shortcode_section_headline',

    'divider' => 'ishyoboy_shortcode_divider',

    'separator' => 'ishyoboy_shortcode_separator',
    'hr' => 'ishyoboy_shortcode_separator',

    'list' => 'ishyoboy_shorcode_list',
    'list_nested1' => 'ishyoboy_shortcode_list',
    'list_nested2' => 'ishyoboy_shortcode_list',
    'list_nested3' => 'ishyoboy_shortcode_list',

    'quote' => 'ishyoboy_shortcode_quote',

    'pullquote' => 'ishyoboy_shortcode_pullquote',

    'author' => 'ishyoboy_shortcode_author',

    'slidable' => 'ishyoboy_shortcode_slidable',
    'slidable_nested1' => 'ishyoboy_shortcode_slidable',
    'slidable_nested2' => 'ishyoboy_shortcode_slidable',
    'slidable_nested3' => 'ishyoboy_shortcode_slidable',

    'slide' => 'ishyoboy_shortcode_slide',
    'slide_nested1' => 'ishyoboy_shortcode_slide',
    'slide_nested2' => 'ishyoboy_shortcode_slide',
    'slide_nested3' => 'ishyoboy_shortcode_slide',

    'the_title' => 'ishyoboy_shortcode_the_title',

    'slider' => 'ishyoboy_shortcode_slider',

    'portfolio' => 'ishyoboy_shortcode_portfolio',
    'portfolio_gallery' => 'ishyoboy_shortcode_portfolio_images',
    'portfolio_categories' => 'ishyoboy_shortcode_portfolio_categories',
    'portfolio_prev_next' => 'ishyoboy_shortcode_portfolio_prev_next',

    'recent_posts' => 'ishyoboy_shortcode_recent_posts',

    'breadcrumbs' => 'ishyoboy_shortcode_breadcrumbs',

    'mark' => 'ishyoboy_shortcode_mark',
    'mark_nested1' => 'ishyoboy_shortcode_mark',
    'mark_nested2' => 'ishyoboy_shortcode_mark',
    'mark_nested3' => 'ishyoboy_shortcode_mark',

    'tooltip' => 'ishyoboy_shortcode_tooltip',
    'tooltip_nested1' => 'ishyoboy_shortcode_tooltip',
    'tooltip_nested2' => 'ishyoboy_shortcode_tooltip',
    'tooltip_nested3' => 'ishyoboy_shortcode_tooltip',

    'skills' => 'ishyoboy_shortcode_skills',
    'skills_nested1' => 'ishyoboy_shortcode_skills',
    'skills_nested2' => 'ishyoboy_shortcode_skills',
    'skills_nested3' => 'ishyoboy_shortcode_skills',

    'skill' => 'ishyoboy_shortcode_skill',
    'skill_nested1' => 'ishyoboy_shortcode_skill',
    'skill_nested2' => 'ishyoboy_shortcode_skill',
    'skill_nested3' => 'ishyoboy_shortcode_skill',

    'dropcap' => 'ishyoboy_shortcode_dropcap',
    'dropcap_nested1' => 'ishyoboy_shortcode_dropcap',
    'dropcap_nested2' => 'ishyoboy_shortcode_dropcap',
    'dropcap_nested3' => 'ishyoboy_shortcode_dropcap',

    'accordion' => 'ishyoboy_shortcode_accordion',
    'accordion_nested1' => 'ishyoboy_shortcode_accordion',
    'accordion_nested2' => 'ishyoboy_shortcode_accordion',
    'accordion_nested3' => 'ishyoboy_shortcode_accordion',

    'acc_item' => 'ishyoboy_shortcode_acc_item',
    'acc_item_nested1' => 'ishyoboy_shortcode_acc_item',
    'acc_item_nested2' => 'ishyoboy_shortcode_acc_item',
    'acc_item_nested3' => 'ishyoboy_shortcode_acc_item',

    'toggle' => 'ishyoboy_shortcode_toggle',
    'toggle_nested1' => 'ishyoboy_shortcode_toggle',
    'toggle_nested2' => 'ishyoboy_shortcode_toggle',
    'toggle_nested3' => 'ishyoboy_shortcode_toggle',

    'tgg_item' => 'ishyoboy_shortcode_tgg_item',
    'tgg_item_nested1' => 'ishyoboy_shortcode_tgg_item',
    'tgg_item_nested2' => 'ishyoboy_shortcode_tgg_item',
    'tgg_item_nested3' => 'ishyoboy_shortcode_tgg_item',

    'pre' => 'ishyoboy_shortcode_pre',
    'pre_nested1' => 'ishyoboy_shortcode_pre',
    'pre_nested2' => 'ishyoboy_shortcode_pre',
    'pre_nested3' => 'ishyoboy_shortcode_pre',

    'code' => 'ishyoboy_shortcode_code',
    'code_nested1' => 'ishyoboy_shortcode_code',
    'code_nested2' => 'ishyoboy_shortcode_code',
    'code_nested3' => 'ishyoboy_shortcode_code',

    'tabs_navigation' => 'ishyoboy_shortcode_tabs_navigation',
    'tabs_navigation_nested1' => 'ishyoboy_shortcode_tabs_navigation',
    'tabs_navigation_nested2' => 'ishyoboy_shortcode_tabs_navigation',
    'tabs_navigation_nested3' => 'ishyoboy_shortcode_tabs_navigation',

    'tabs_content' => 'ishyoboy_shortcode_tabs_content',
    'tabs_content_nested1' => 'ishyoboy_shortcode_tabs_content',
    'tabs_content_nested2' => 'ishyoboy_shortcode_tabs_content',
    'tabs_content_nested3' => 'ishyoboy_shortcode_tabs_content',

    'tab_title' => 'ishyoboy_shortcode_tab_title',
    'tab_title_nested1' => 'ishyoboy_shortcode_tab_title',
    'tab_title_nested2' => 'ishyoboy_shortcode_tab_title',
    'tab_title_nested3' => 'ishyoboy_shortcode_tab_title',

    'tab_content' => 'ishyoboy_shortcode_tab_content',
    'tab_content_nested1' => 'ishyoboy_shortcode_tab_content',
    'tab_content_nested2' => 'ishyoboy_shortcode_tab_content',
    'tab_content_nested3' => 'ishyoboy_shortcode_tab_content',

    'content_break' => 'ishyoboy_shortcode_content_break',
    'content_break_nested1' => 'ishyoboy_shortcode_content_break',
    'content_break_nested2' => 'ishyoboy_shortcode_content_break',
    'content_break_nested3' => 'ishyoboy_shortcode_content_break',

    'page_break' => 'ishyoboy_shortcode_page_break',
    'page_break_nested1' => 'ishyoboy_shortcode_page_break',
    'page_break_nested2' => 'ishyoboy_shortcode_page_break',
    'page_break_nested3' => 'ishyoboy_shortcode_page_break',

    'section' => 'ishyoboy_shortcode_section',
    'section_nested1' => 'ishyoboy_shortcode_section',
    'section_nested2' => 'ishyoboy_shortcode_section',
    'section_nested3' => 'ishyoboy_shortcode_section',

    'table' => 'ishyoboy_shortcode_table',
    'table_nested1' => 'ishyoboy_shortcode_table',
    'table_nested2' => 'ishyoboy_shortcode_table',
    'table_nested3' => 'ishyoboy_shortcode_table',

    'pricing_table' => 'ishyoboy_shortcode_pricing_table',

    'pricing_row' => 'ishyoboy_shortcode_pricing_row',

    'chart' => 'ishyoboy_shortcode_chart',

    'timeline' => 'ishyoboy_shortcode_timeline',

    'timeline_item' => 'ishyoboy_shortcode_timeline_item',

    'timeline_date' => 'ishyoboy_shortcode_timeline_date',

    'timeline_content' => 'ishyoboy_shortcode_timeline_content',

    'rounded_image' => 'ishyoboy_shortcode_rounded_image',
    'featured_image' => 'ishyoboy_shortcode_featured_image',

    'navigation' => 'ishyoboy_shortcode_navigation',
    'menu' => 'ishyoboy_shortcode_navigation',
    'nav' => 'ishyoboy_shortcode_navigation',

    'map' => 'ishyoboy_shortcode_map',
);


// Helper function to create all columns based on the class
if ( !function_exists('ishyoboy_shortcode_create_column') ) {
function ishyoboy_shortcode_create_column($class, $atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
    ), $atts);

    $return = '';

    $return .= '<div class="' . $class;
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']) . '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// Full = grid12
if ( !function_exists('ishyoboy_shortcode_grid12') ) {
function ishyoboy_shortcode_grid12($atts, $content = null) { return ishyoboy_shortcode_create_column('grid12', $atts, $content); }
}

// grid11
if ( !function_exists('ishyoboy_shortcode_grid11') ) {
function ishyoboy_shortcode_grid11($atts, $content = null) { return ishyoboy_shortcode_create_column('grid11', $atts, $content); }
}

// grid10
if ( !function_exists('ishyoboy_shortcode_grid10') ) {
function ishyoboy_shortcode_grid10($atts, $content = null) { return ishyoboy_shortcode_create_column('grid10', $atts, $content); }
}

// grid9
if ( !function_exists('ishyoboy_shortcode_grid9') ) {
function ishyoboy_shortcode_grid9($atts, $content = null) { return ishyoboy_shortcode_create_column('grid9', $atts, $content); }
}

// grid8
if ( !function_exists('ishyoboy_shortcode_grid8') ) {
function ishyoboy_shortcode_grid8($atts, $content = null) { return ishyoboy_shortcode_create_column('grid8', $atts, $content); }
}

// grid7
if ( !function_exists('ishyoboy_shortcode_grid7') ) {
function ishyoboy_shortcode_grid7($atts, $content = null) { return ishyoboy_shortcode_create_column('grid7', $atts, $content); }
}

// grid6
if ( !function_exists('ishyoboy_shortcode_grid6') ) {
function ishyoboy_shortcode_grid6($atts, $content = null) { return ishyoboy_shortcode_create_column('grid6', $atts, $content); }
}

// grid5
if ( !function_exists('ishyoboy_shortcode_grid5') ) {
function ishyoboy_shortcode_grid5($atts, $content = null) { return ishyoboy_shortcode_create_column('grid5', $atts, $content); }
}

// grid4
if ( !function_exists('ishyoboy_shortcode_grid4') ) {
function ishyoboy_shortcode_grid4($atts, $content = null) { return ishyoboy_shortcode_create_column('grid4', $atts, $content); }
}

// grid3
if ( !function_exists('ishyoboy_shortcode_grid3') ) {
function ishyoboy_shortcode_grid3($atts, $content = null) { return ishyoboy_shortcode_create_column('grid3', $atts, $content); }
}

// grid2
if ( !function_exists('ishyoboy_shortcode_grid2') ) {
function ishyoboy_shortcode_grid2($atts, $content = null) { return ishyoboy_shortcode_create_column('grid2', $atts, $content); }
}

// grid1
if ( !function_exists('ishyoboy_shortcode_grid1') ) {
function ishyoboy_shortcode_grid1($atts, $content = null) { return ishyoboy_shortcode_create_column('grid1', $atts, $content); }
}

// BOX & ICONIC BOX
if ( !function_exists('ishyoboy_shortcode_box') ) {
function ishyoboy_shortcode_box($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'icon' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="box';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' iconic' : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';
    $return .= ('' != $sc_atts['icon']) ? '<div class="box-icon ' . esc_attr($sc_atts['icon']) . '"></div>' : '' ;

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// ALERT BOX
if ( !function_exists('ishyoboy_shortcode_alert') ) {
function ishyoboy_shortcode_alert($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'icon' => '',
        'type' => '',
        'closable' => 'no'
    ), $atts);

    $return = '';

    $return .= '<div class="box';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' iconic' : '' ;
    $return .= ('' != $sc_atts['type']) ? ' ' . esc_attr($sc_atts['type']) : '' ;
    $return .= ('yes' == $sc_atts['closable']) ? ' close' : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';
    $return .= ('' != $sc_atts['icon']) ? '<div class="box-icon ' . esc_attr($sc_atts['icon']) . '"></div>' : '' ;

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// BOX_GROUP
if ( !function_exists('ishyoboy_shortcode_box_group') ) {
function ishyoboy_shortcode_box_group($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'align' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="boxgroup';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// ROW
if ( !function_exists('ishyoboy_shortcode_row') ) {
function ishyoboy_shortcode_row($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="row';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// BUTTON
if ( !function_exists('ishyoboy_shortcode_button') ) {
function ishyoboy_shortcode_button($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'bg_color' => '',
        'text_color' => '',
        'full_width' => '',
        'size' => 'small',
        'new_window' => 'no',
        'url' => '',
        'align' => ''
    ), $atts);

    $return = '';

    $return .= '<a class="ish-button';

    $return .= ('small' == $sc_atts['size']) ? '-' . $sc_atts['size'] : '' ;
    $return .= ('medium' == $sc_atts['size']) ? '-' . $sc_atts['size'] : '' ;
    $return .= ('big' == $sc_atts['size']) ? '-' . $sc_atts['size'] : '' ;

    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['full_width']) ? ' button-fullwidth' : '' ;
    $return .= ('' != $sc_atts['bg_color'] || '' != $sc_atts['text_color']) ? ' button-advanced' : '' ;

    $return .= ('right' == $sc_atts['align']) ? ' right' : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' center' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' left' : '' ;

    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;

    $return .= '"' ;

    $return .= ('' != $sc_atts['url']) ? ' href="' . esc_attr( apply_filters( 'ishyoboy_sc_url', $sc_atts['url'] ) ) . '"' : '';

    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';

    if ('' != $sc_atts['style'] || '' != $sc_atts['bg_color'] || '' != $sc_atts['text_color']){
        $return .= ' style="';

        $return .= ('' != $sc_atts['bg_color']) ? ' background-color: ' . esc_attr($sc_atts['bg_color']) . ' !important;' : '';
        $return .= ('' != $sc_atts['text_color']) ? ' color: ' . esc_attr($sc_atts['text_color']) . ' !important;'  : '';
        $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';

        $return .= '"';

    }

    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= ('yes' == $sc_atts['new_window']) ? ' target="_blank"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</a>';

    return $return ;
}
}

// ICON
if ( !function_exists('ishyoboy_shortcode_icon') ) {
function ishyoboy_shortcode_icon($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'type' => '',
        'align' => '',
        'icon' => '',
        'size' => '',
        'url' => '',
        'new_window' => ''
    ), $atts);

    $return = '';

    $return .= ('' != $sc_atts['type']) ? '<span class="ish-icon-' . esc_attr($sc_atts['type']) : '<span class="ish-icon' ;
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ' style="';
    $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';
    $return .= '"';

    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    if ('' != $sc_atts['url']) {
        $return .= '<a href="' . esc_attr( apply_filters( 'ishyoboy_sc_url', $sc_atts['url'] ) ) . '"';
        $return .= ('yes' == $sc_atts['new_window']) ? ' target="_blank"' : ''  ;
        $return .= '><span class="';
    }
    else {
        $return .= '<span class="';
    }

    $return .= ('' != $sc_atts['icon']) ? esc_attr($sc_atts['icon']) . '" ' : '"' ;

    if ('' != $sc_atts['size']) {
        $return .= 'style="';

        switch ($sc_atts['type']) {
            case 'square':
                $offst = 18;
                $size = esc_attr($sc_atts['size']) + $offst;

                $return .= 'font-size:' . esc_attr($sc_atts['size']) . 'px;';

                $return .= 'width:' . $size . 'px;';
                $return .= 'height:' . $size . 'px;';

                if ( esc_attr($sc_atts['size']) < $offst ) {
                    $return .= 'padding-top:' . esc_attr($sc_atts['size']) / 2 . 'px !important;';
                }
                else {
                    $return .= 'padding-top:' . $offst / 2 . 'px !important;';
                }

                break;

            case 'circle':
                $offst = 20;
                $size = esc_attr($sc_atts['size']) + $offst;

                $return .= 'font-size:' . esc_attr($sc_atts['size'] - 5) . 'px !important;';

                $return .= 'width:' . $size . 'px;';
                $return .= 'height:' . $size . 'px;';

                $return .= '-moz-border-radius:' . $size / 2 . 'px;';
                $return .= '-webkit-border-radius:' . $size / 2 . 'px;';
                $return .= 'border-radius:' . $size / 2 . 'px;';

                if ( esc_attr($sc_atts['size']) < $offst ) {
                    $return .= 'padding-top:' . esc_attr($sc_atts['size']) / 2 . 'px !important;';
                }
                else {
                    $return .= 'padding-top:' . ($offst / 2 + 2.5) . 'px !important;';
                }

                break;

            default:
                $offst = 4;
                $size = esc_attr($sc_atts['size']) + $offst;

                $return .= 'font-size:' . esc_attr($sc_atts['size']) . 'px;';

                $return .= 'width:' . $size . 'px;';
                $return .= 'height:' . $size . 'px;';

                $return .= 'padding-top:' . $offst / 2 . 'px !important;';

                break;
        }

        $return .= '"';
    }

    $return .=  '></span>';
    $return .= ('' != $sc_atts['url']) ? '</a>' : '';

    $return .= '</span>';

    return $return ;
}
}

// SOCIAL LINK
if ( !function_exists('ishyoboy_shortcode_social') ) {
function ishyoboy_shortcode_social($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'icon' => '',
        'url' => '',
        'title' => '',
        'text_color' => '',
        'new_window' => 'yes'
    ), $atts);

    $return = '';

    $return .= '<a class="ish-social';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['text_color']) ? ' social-advanced' : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' ' . esc_attr($sc_atts['icon']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';

    if ('' != $sc_atts['style'] || '' != $sc_atts['text_color']){
        $return .= ' style="';

        $return .= ('' != $sc_atts['text_color']) ? ' color: ' . esc_attr($sc_atts['text_color']) : '';
        $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';

        $return .= '"';

    }

    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= ('' != $sc_atts['title'] && '' == $sc_atts['tooltip'] ) ? ' title="' . esc_attr($sc_atts['title']) . '"' : '';
    $return .= ('' != $sc_atts['url']) ? ' href="' . esc_attr( apply_filters( 'ishyoboy_sc_url', $sc_atts['url'] ) ) . '"' : '';
    $return .= ('yes' == $sc_atts['new_window']) ? ' target="_blank"' : ''  ;
    $return .= '><span>';

    $return .= ('' != $sc_atts['tooltip']) ? esc_attr($sc_atts['tooltip']) : ''  ;
    $return .= ('' != $sc_atts['title'] && '' == $sc_atts['tooltip'] ) ? esc_attr($sc_atts['title']) : ''  ;

    $return .= '</span></a>';

    return $return ;
}
}

// SOCIAL SHARE
if ( !function_exists('ishyoboy_shortcode_social_share') ) {
function ishyoboy_shortcode_social_share($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => ''
    ), $atts);

    $return = '';

    global $ish_options;

    if (isset($ish_options['addthis_share']) && '' != $ish_options['addthis_share'] ){

        $return .= '<div class="share_box';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('center' == $sc_atts['align']) ? ' center' : '' ;
        $return .= ('right' == $sc_atts['align']) ? ' right' : '' ;
        $return .= ('left' == $sc_atts['align']) ? ' left' : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;

        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        $return .= str_replace(array("\r\n", "\n", "\r", "<br />", "<br>"), '', $ish_options['addthis_share']);

        $return .= '</div>';

    }

    return $return ;
}
}

// HEADLINE
if ( !function_exists('ishyoboy_shortcode_headline') ) {
function ishyoboy_shortcode_headline($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'tag' => 'h4',
        'color' => '',
        'lined' => 'no',
        'icon' => ''
    ), $atts);

    $return = '';

    $return .= '<' . $sc_atts['tag'] . ' class="';
    $return .= ('yes' == $sc_atts['lined']) ? 'lined' : '' ;
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' ' . esc_attr($sc_atts['icon']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content );
    $return .= ('yes' == $sc_atts['lined']) ? '<span></span>' : '' ;
    $return .= '</' . $sc_atts['tag'] . '>';

    return $return ;
}
}

// SECTION HEADLINE
if ( !function_exists('ishyoboy_shortcode_section_headline') ) {
function ishyoboy_shortcode_section_headline($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'tag' => 'h4',
        'color' => '',
        'lined' => 'no',
        'icon' => ''
    ), $atts);

    $return = '';

    $return .= '<' . $sc_atts['tag'] . ' class="';
    $return .= ('yes' == $sc_atts['lined']) ? 'lined-section' : '' ;
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' ' . esc_attr($sc_atts['icon']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '<span></span></' . $sc_atts['tag'] . '>';

    return $return ;
}
}

// Divider
if ( !function_exists('ishyoboy_shortcode_divider') ) {
function ishyoboy_shortcode_divider($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="space';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= '</div>';

    return $return ;
}
}

// Separator
if ( !function_exists('ishyoboy_shortcode_separator') ) {
function ishyoboy_shortcode_separator($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="lined-section-only';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= '<span></span></div>';

    return $return ;
}
}

// LIST
if ( !function_exists('ishyoboy_shorcode_list') ) {
function ishyoboy_shorcode_list($atts, $content=null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'type' => '',
        'align' => ''
    ), $atts);

    /*
    ul.list-square li,
    ul.list-square-empty li,
    ul.list-thick li,
    ul.list-dot li,
    ul.list-circle li,
    ul.list-cancel li,
    ul.list-plus li,
    ul.list-minus li,
    ul.list-pointer
    */

    $return = '';

    $return .= '<ul class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['type']) ? ' list-' . esc_attr($sc_atts['type']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $content = preg_replace('/<ul>/', $return, $content, 1);
    //$content = str_replace('<ul>', $return, $content);
    $return = '';

    $return .= do_shortcode( $content );

    return $return ;
}
}

// QUOTE, BLOCKQUOTES
if ( !function_exists('ishyoboy_shortcode_quote') ) {
function ishyoboy_shortcode_quote($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => 'color1',
        'align' => '',
        'boxed' => 'no'
    ), $atts);

    $return = '';

    $return .= '<blockquote class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['boxed']) ? ' quote-boxed' : '' ;
    //$return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    //$return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    //$return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content );

    $return .= '</blockquote>';

    return $return ;
}
}

// PULLQUOTE
if ( !function_exists('ishyoboy_shortcode_pullquote') ) {
function ishyoboy_shortcode_pullquote($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'last' => 'no',
        'color' => 'color1',
        'align' => 'left',
        'boxed' => 'no',
    ), $atts);

    $return = '';

    $return .= '<div class="pullquote';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('yes' == $sc_atts['last']) ? ' last' : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['boxed']) ? ' bg-pullquote' : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' center' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' left' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content );

    $return .= '</div>';

    return $return ;
}
}

// AUTHOR
if ( !function_exists('ishyoboy_shortcode_author') ) {
function ishyoboy_shortcode_author($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => 'color1',
        'align' => '',
    ), $atts);

    $return = '';

    $return .= '<cite class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content );

    $return .= '</cite>';

    return $return ;
}
}

// SLIDABLE
if ( !function_exists('ishyoboy_shortcode_slidable') ) {
function ishyoboy_shortcode_slidable($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'align' => '',
        'autoslide' => '',
        'animation' => '',
        'interval' => '',
        'navigation' => '',
        //'sameheight' => '',
    ), $atts);

	wp_enqueue_script( 'imagesloaded' );
	wp_enqueue_script( 'flexslider' );
	//wp_enqueue_script( 'isotope' );

    $return = '';

    $return .= '<div class="slidable';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : '' ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : '' ;
    $return .= ('yes' == $sc_atts['autoslide']) ? ' data-autoslide="yes"' : '' ;
    $return .= ('fade' == $sc_atts['animation']) ? ' data-animation="fade"' : '' ;
    $return .= ('' != $sc_atts['interval']) ? ' data-interval="' . esc_attr($sc_atts['interval']) . '"' : '' ;
    $return .= ('no' == $sc_atts['navigation']) ? ' data-navigation="no"' : '' ;
    //$return .= ('yes' == $sc_atts['sameheight']) ? ' data-sameheight="yes"' : '' ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// SLIDE
if ( !function_exists('ishyoboy_shortcode_slide') ) {
function ishyoboy_shortcode_slide($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'align' => ''
    ), $atts);

	wp_enqueue_script( 'imagesloaded' );
	wp_enqueue_script( 'flexslider' );
	//wp_enqueue_script( 'isotope' );

    $return = '';

    $return .= '<div class="slide';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// THE TITLE
if ( !function_exists('ishyoboy_shortcode_the_title') ) {
function ishyoboy_shortcode_the_title($atts, $content = null){
    global $post ,$page;

    $return = '';

    if ( is_page() || is_home() ){
        //$return .= 'PAGE: ';

        if (is_object($page)){
            //$return .= 'page - ';
            $return = $page->post_title;
        }else{
            if (is_object($post)){
                //$return .= 'not - ';
                $return = $post->post_title;
            }
            else {
                // Absolutely not

            }

        }

    }else{

        if ( function_exists('is_woocommerce') && ( is_shop()) ){

            $shop_page_id = woocommerce_get_page_id( 'shop' );
            $page_title   = get_the_title( $shop_page_id );
            $p = get_post($shop_page_id);
            $return = $p->post_title;
        }
        else{
            //$return .= 'POST: ';
            $return = get_the_title();
        }
    }

    return $return;
}
}

// SLIDER
if ( !function_exists('ishyoboy_shortcode_slider') ) {
function ishyoboy_shortcode_slider($atts, $content=null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'slider_name' => '',
        'align' => '',
        'autoslide' => '',
        'animation' => '',
        'interval' => '',
        'navigation' => '',
        'height' => ''
    ), $atts);

    $cat = get_term_by('slug', $sc_atts['slider_name'], 'slides_categories');
    $cat_id = $cat->term_id;
    if ($cat_id){

        $tax_query = array(
            array(
                'taxonomy' => 'slides_categories',
                'field' => 'id',
                'terms' => $cat_id
            )
        );

    }
    else{

        $tax_query = array(
            array(
                'taxonomy' => 'slides_categories',
                'field' => 'slug',
                'terms' => $sc_atts['slider_name']
            )
        );

    }

    // Get all Slides posts
    $wpbp = new WP_Query( array(
            'post_type' =>  'ishyoboy_slides',
            'posts_per_page'  => '-1',
            'post_status' => 'publish',
            'orderby' => 'menu_order',
            'order' => 'ASC',
            'tax_query' => $tax_query
        )
    );

    $return = '';

    if ($wpbp->have_posts()) {
        global $slides;

	    wp_enqueue_script( 'imagesloaded' );
	    wp_enqueue_script( 'flexslider' );
	    //wp_enqueue_script( 'isotope' );

        $return .= '<div class="ish-slider slidable';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
        $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
        $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= ('yes' == $sc_atts['autoslide']) ? ' data-autoslide="yes"' : '' ;
        $return .= ('fade' == $sc_atts['animation']) ? ' data-animation="fade"' : '' ;
        $return .= ('' != $sc_atts['interval']) ? ' data-interval="' . esc_attr($sc_atts['interval']) . '"' : '' ;
        $return .= ('no' == $sc_atts['navigation']) ? ' data-navigation="no"' : '' ;
        $return .= '>' . "\n";

        $slides = '';

        if ( '' != $sc_atts['height'] && is_numeric($sc_atts['height']) ) {
            $image_slide_height = ' style="max-height: ' . $sc_atts['height'] . 'px;"';
            $content_slide_height = ' style="min-height: ' . $sc_atts['height'] . 'px;"';
        }
        else{
            $image_slide_height = '';
            $content_slide_height = '';
        }

        while ($wpbp->have_posts()) {
            global $slides;

            $wpbp->the_post();
            $id = get_the_ID();

            $type =  IshYoMetaBox::get('ishyoboy_slide_type', true, $id);

            if ('image' == $type){

                $slides .= '<div class="slide-image"' . $image_slide_height . '>' . "\n";

                $url = IshYoMetaBox::get('ishyoboy_slide_url', true, $id);
                $url_new_window = IshYoMetaBox::get('ishyoboy_slide_url_new_window', true, $id);

                if ( isset($url) && '' != $url ){
                    // HAS URL SET
                    $slides .= '<a href="' . esc_attr( apply_filters( 'ishyoboy_sc_url', $url ) ) . '"';

                    if ( isset($url_new_window) && 'true' == $url_new_window ){
                        // OPEN IN NEW WINDOW
                        $slides .= ' target="_blank"';
                    }

                    $slides .= ' >';

                }

                if ( (function_exists('has_post_thumbnail')) && (has_post_thumbnail()) ) {

                    $slides .= get_the_post_thumbnail( $id , 'full', array(  'class' => ''));
                }

                $slides .= '<div class="caption">';
                $slides .= do_shortcode(get_the_content());
                $slides .= '</div>' . "\n";

                if ( isset($url) && '' != $url ){
                    // HAS URL SET
                    $slides .= '</a>';
                }

                $slides .= '</div>' . "\n";
            }
            else{

                $src = '';

                if ( (function_exists('has_post_thumbnail')) && (has_post_thumbnail()) ) {
                    $img_details = wp_get_attachment_image_src( get_post_thumbnail_id( $id ), 'full');
                    $src = ' style="background-image: url(\'' . $img_details[0] . '\');"';
                }

                $slides .= '<div class="slide-content"' . $src . '><div class="row"' . $content_slide_height . '>' . "\n";

                $slides .= do_shortcode(get_the_content());

                $slides .= '</div></div>' . "\n";

            }

        }

        $return .= $slides;
        $return .= '</div>';
    }

    wp_reset_query();

    return $return;

}
}

$sc_count = 0;
$sc_paginated_count = 0;

// PORTFOLIO
if ( !function_exists('ishyoboy_shortcode_portfolio') ) {
function ishyoboy_shortcode_portfolio($atts, $content=null){

    global $sc_count, $sc_paginated_count, $ish_options;

    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'category' => '',
        'height' => (isset($ish_options['portfolio_height'])) ? $ish_options['portfolio_height'] : '',
        'per_page' => (isset($ish_options['portfolio_posts_per_page']) && is_numeric($ish_options['portfolio_posts_per_page'])) ? $ish_options['portfolio_posts_per_page'] : '-1' ,
        'order' => 'DESC',
        'navigation' => 'yes',
        'pagination' => 'yes',
        'view_all' => 'no',
        'columns' => (isset($ish_options['portfolio_columns'])) ? $ish_options['portfolio_columns'] : '4',
        'masonry' => (isset($ish_options['portfolio_masonry'])) ? $ish_options['portfolio_masonry'] : '0',

        'fluid_layout' => (isset($ish_options['portfolio_fluid_layout'])) ? ( ( '1' == $ish_options['portfolio_fluid_layout']) ? 'yes' : 'no' ) : 'no',
	    'layout_style' => (isset($ish_options['portfolio_layout_style'])) ? $ish_options['portfolio_layout_style'] : '1',
        'show_title' => (isset($ish_options['portfolio_show_title'])) ? ( ( '1' == $ish_options['portfolio_show_title']) ? 'yes' : 'no' ) : 'yes',
        'show_categories' => (isset($ish_options['portfolio_show_categories'])) ? ( ( '1' == $ish_options['portfolio_show_categories']) ? 'yes' : 'no' ) : 'yes',
        'show_link_button' => (isset($ish_options['portfolio_show_link_button'])) ? ( ( '1' == $ish_options['portfolio_show_link_button']) ? 'yes' : 'no' ) : 'yes',
        'show_popup_button' => (isset($ish_options['portfolio_show_popup_button'])) ? ( ( '1' == $ish_options['portfolio_show_popup_button']) ? 'yes' : 'no' ) : 'yes',
        'animate_filter' => (isset($ish_options['portfolio_animate_filter'])) ? ( ( '1' == $ish_options['portfolio_animate_filter']) ? 'yes' : 'no' ) : 'no',

    ), $atts);

    $sc_count++;

    wp_enqueue_script( 'imagesloaded' );
	wp_enqueue_script( 'isotope' );

    $p_return = '<div>';

    // Default type of connecting multiple taxonomy "filters". Do not change this!
    $tax_query = array(
        'relation' => 'AND'
    );

    if (isset($sc_atts['category']) && '' != $sc_atts['category']){
        // Display items from a chosen category

        $cats = explode( ',', $sc_atts['category'] );
        $cat_ids = array();
        $neg_cat_ids = array();

        foreach ( $cats as $cat_slug ){
            $cat_slug = trim($cat_slug);

            if ( '-' == $cat_slug[0] ){
                // Negative categories = exclude those
                $cat = get_term_by( 'slug', $cat_slug, 'portfolio-category' );
                if ($cat) $neg_cat_ids[] = $cat->term_id;
            }
            else{
                // Positive categories
                $cat = get_term_by( 'slug', $cat_slug, 'portfolio-category' );
                if ($cat) $cat_ids[] = $cat->term_id;
            }
        }

        if ( count( $cat_ids ) > 0 || count( $neg_cat_ids ) > 0 ){

            if ( count( $cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'portfolio-category',
                    'field' => 'id',
                    'terms' => $cat_ids
                );
            }

            if ( count( $neg_cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'portfolio-category',
                    'field' => 'id',
                    'terms' => $neg_cat_ids,
                    'operator' => 'NOT IN'
                );
            }

        }
        else {

            $tax_query[] = array(
                'taxonomy' => 'portfolio-category',
                'field' => 'slug',
                'terms' => $cats
            );

        }

    }
    else{
        // Display items from all categories
        // $tax_query = array();
    }

    if ('yes' == $sc_atts['navigation'] ){

        $terms = array();

        if (isset($sc_atts['category']) && '' != $sc_atts['category']){

            $cats = explode( ',', $sc_atts['category'] );
            $parent_ids = array();
            $neg_parent_ids = array();

            foreach ( $cats as $cat_slug ){
                $cat_slug = trim($cat_slug);

                if ( '-' == $cat_slug[0] ){
                    // Negative categories = exclude those
                    $parent = get_term_by( 'slug', $cat_slug, 'portfolio-category' );
                    if ($parent) $neg_parent_ids[] = $parent->term_id;
                }
                else{
                    // Positive categories
                    $parent = get_term_by( 'slug', $cat_slug, 'portfolio-category' );
                    if ($parent) $parent_ids[] = $parent->term_id;
                }
            }

            if ( count($parent_ids) <= 0 ){
                $t = array();
                $t = get_terms('portfolio-category', array(
                    'parent'     => 0,
                    'hide_empty' => 1,
                    'hierarchical' => 0,
                    'exclude' => $neg_parent_ids,
                    'exclude_tree' => $neg_parent_ids,
                    'taxonomy' => 'portfolio-category'
                ));

                $terms = array_merge( $terms, $t);
            }
            else{

                foreach ( $parent_ids as $pid ){

                    $t = array();
                    $t = get_terms('portfolio-category', array(
                        'parent'     => $pid,
                        'hide_empty' => 1,
                        'hierarchical' => 0,
                        'exclude' => $neg_parent_ids,
                        'exclude_tree' => $neg_parent_ids,
                        'taxonomy' => 'portfolio-category'
                    ));

                    $terms = array_merge( $terms, $t);
                }
            }

        }
        else{
            $terms = get_terms('portfolio-category', array(
                'parent'     => 0,
                'hide_empty' => 0,
                'hierarchical' => 1,
                'taxonomy' => 'portfolio-category'
            ));
        }

        $count = count($terms);
        $i=0;
        if ($count > 0) {

            if ( 'yes' == $sc_atts['animate_filter']){
                $p_return .= '<ul class="list-button portfolio-filters">';
                $p_return .= '<li><a href="#" data-filter="*">' . __( 'All', 'ishyoboy' ) . '</a></li>';
            }else{
                $p_return .= '<ul class="list-button">';
            }


            $term_list = '';
            foreach ($terms  as $term) {
                $i++;

                if ( 'yes' == $sc_atts['animate_filter']){
                    $term_list .= '<li><a href="' . get_term_link($term->slug, 'portfolio-category') . '" data-filter=".pfilt-' . $term->slug . '">' . $term->name . '</a></li>';
                }
                else{
                    $term_list .= '<li><a href="' . get_term_link($term->slug, 'portfolio-category') . '">' . $term->name . '</a></li>';
                }
            }
            $p_return .= $term_list;
            $p_return .= '</ul>';
        }

    }

    $thumb_size = 'theme-large';

    $grid = 'grid3';

    if (is_numeric($sc_atts['columns'])){
        switch ($sc_atts['columns']) {
            case '4' :
                $grid = 'grid3';
                $thumb_size = 'theme-half';
                break;
            case '3' :
                $grid = 'grid4';
                $thumb_size = 'theme-half';
                break;
            case '2' :
                $grid = 'grid6';
                $thumb_size = 'theme-large';
                break;
            case '1' :
                $grid = 'grid12';
                $thumb_size = 'theme-large';
                break;
            case '5' :
                $grid = 'grid2';
                $thumb_size = 'theme-fourth';
                break;
            case '6' :
                $grid = 'grid2';
                $thumb_size = 'theme-fourth';
                break;
            default :
                $grid = 'grid3';
                $thumb_size = 'theme-half';
        }
    }

    $paged = 1;

    $pagin_type = 'pagination';

    if ('yes' == $sc_atts['view_all'] && '' != $sc_atts['category']){
        $pagin_type = 'view_all';
    }
    if ('yes' == $sc_atts['pagination'] && '' == $sc_atts['category']){
        $pagin_type = 'pagination';
    }
    if ('pagination' == $pagin_type  && 'yes' != $sc_atts['pagination']){
        $pagin_type = '';
    }

    if ( ('pagination' == $pagin_type) && is_numeric($sc_atts['per_page']) && (int)$sc_atts['per_page'] > 0 ){

        $sc_paginated_count++;

        if ($sc_paginated_count < 2){

            if (is_front_page()){
                $paged = (get_query_var('page')) ? get_query_var('page') : 1;
            }
            else{
                $paged = (get_query_var('paged')) ? get_query_var('paged') : 1;

            }
        }
    }

    $order = ('ASC' == strtoupper($sc_atts['order'])) ? 'ASC' : 'DESC' ; // ASC OR DESC

    // Get all Portfolio posts
    $wpbp = new WP_Query( array(
            'post_type' =>  'portfolio-post',
            'posts_per_page'  => $sc_atts['per_page'],
            'paged' => $paged,
            'order' => $order,
            'post_status' => 'publish',
            'tax_query' => $tax_query
        )
    );

    if ($wpbp->have_posts()) :

        $portfolio_classes = '';
        if ( 'yes' == $sc_atts['fluid_layout'] ) {
            $portfolio_classes .= ' portfolio-fluid';
        }else{
            $portfolio_classes .= ' portfolio-fixed';
        }

        if ('yes' == $sc_atts['animate_filter']) {
            $portfolio_classes .= ' anim-filter';
        } else {
            $portfolio_classes .= ' noanim-filter';
        }


        switch ( $sc_atts['layout_style'] ){
            case '2' :
                $portfolio_classes .= ' portfolio-style-2';
                break;

            case '3' :
                $portfolio_classes .= ' portfolio-style-3';
                break;

            case '4' :
                $portfolio_classes .= ' portfolio-style-4';
                break;

            default :
                $portfolio_classes .= ' portfolio-style-1';
                break;
        }

        if ('1' == $sc_atts['masonry'] || 'yes' == $sc_atts['masonry']) {
            $p_return .= '<div class="row portfoliog masonry' . $portfolio_classes . '"><div class="mass_content">';
        } else{
            $p_return .= '<div class="row portfoliog nomasonry' . $portfolio_classes . '">';
        }

        while ($wpbp->have_posts()) : $wpbp->the_post();

            $terms = get_the_terms(  get_the_ID(), 'portfolio-category' );

            $p_return .= '<div class="portfolio-item ' . $grid;

            $p_return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;

            if ( 'yes' == $sc_atts['animate_filter']) {
                if (isset($terms) && '' != $terms ) { foreach ($terms as $term) { $p_return .= ' pfilt-' . esc_attr($term->slug); } }
            }

            $p_return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
            $p_return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
            $p_return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
            $p_return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
            $p_return .= '"' ;
            $p_return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
            $p_return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
            $p_return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
            $p_return .= '>';

            if ('3' == $sc_atts['layout_style']) {

                // TITLE
                if ( 'yes' == $sc_atts['show_title'] ) {
                    $p_return .= '<h3><a href="' . get_permalink() . '">' . get_the_title() . '</a></h3>';
                }

                // CATEGORIES
                if ( 'yes' == $sc_atts['show_categories'] ) {
                    if (isset($terms) && '' != $terms ) {
                        $p_return .= '<ul class="icon-folder">';
                        foreach ($terms as $term) {
                            $p_return .= '<li><a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a></li>' . "\n";
                        }
                        $p_return .= '</ul>';
                    }
                }

            }

            if ('4' == $sc_atts['layout_style']) {

                // TITLE
                if ( 'yes' == $sc_atts['show_title'] ) {
                    $p_return .= '<h3><a href="' . get_permalink() . '">' . get_the_title() . '</a></h3>';
                }

            }

            if ('' != $sc_atts['height']) {
                $p_return .= '    <div class="hover-cont" style="max-height: ' . esc_attr($sc_atts['height']) . 'px;">';
            } else{
                $p_return .= '    <div class="hover-cont">';
            }


            if ( (function_exists('has_post_thumbnail')) && (has_post_thumbnail()) ) {
                $p_return .=  get_the_post_thumbnail(get_the_ID(), $thumb_size);
            }

            if ( '1' != $sc_atts['layout_style'] && 'no' == $sc_atts['show_link_button'] && 'no' == $sc_atts['show_popup_button'] ) {
                $p_return .= '<div class="hover-overlay hover-empty">';
                $p_return .= '<a href="' . get_permalink() . '" class="hover-full"><span><span class="icon-link"></span></span></a>';
            }
            else{
                $p_return .= '<div class="hover-overlay">';
            }

            if ('1' == $sc_atts['layout_style']) {

                // TITLE
                if ( 'yes' == $sc_atts['show_title'] ) {
                    $p_return .= '<h3><a href="' . get_permalink() . '">' . get_the_title() . '</a></h3>';
                }

                // CATEGORIES
                if ( 'yes' == $sc_atts['show_categories'] ) {
                    if (isset($terms) && '' != $terms ) {
                        $p_return .= '<ul class="icon-folder">';
                        foreach ($terms as $term) {
                            $p_return .= '<li><a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a></li>' . "\n";
                        }
                        $p_return .= '</ul>';
                    }
                }

            }

            // BUTTONS
            if ( 'yes' == $sc_atts['show_link_button'] || 'yes' == $sc_atts['show_popup_button'] ) {
                $p_return .= '<div class="hover-links">';

                // LINK BUTTON
                if ( 'yes' == $sc_atts['show_link_button']) {
                    $p_return .= '    <a href="' . get_permalink() . '" class="ish-button-small icon-link"></a>';
                }

                // POPUP BUTTON
                if ( 'yes' == $sc_atts['show_popup_button'] ) {
                    $img_details = wp_get_attachment_image_src( get_post_thumbnail_id(get_the_ID()), 'full' );
                    $p_return .= '<a href="' . esc_attr($img_details[0]) . '" rel="portfolio-sc-' . $sc_count . '" class="openfancybox-image ish-button-small icon-search-4"></a>';
                }

                $p_return .= '</div>';
            }

            $p_return .= '</div></div>';

            if ('2' == $sc_atts['layout_style']) {

                // TITLE
                if ( 'yes' == $sc_atts['show_title'] ) {
                    $p_return .= '<h3><a href="' . get_permalink() . '">' . get_the_title() . '</a></h3>';
                }

                // CATEGORIES
                if ( 'yes' == $sc_atts['show_categories'] ) {
                    if (isset($terms) && '' != $terms ) {
                        $p_return .= '<ul class="icon-folder">';
                        foreach ($terms as $term) {
                            $p_return .= '<li><a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a></li>' . "\n";
                        }
                        $p_return .= '</ul>';
                    }
                }

            }

            if ('4' == $sc_atts['layout_style']) {

                // CATEGORIES
                if ( 'yes' == $sc_atts['show_categories'] ) {
                    if (isset($terms) && '' != $terms ) {
                        $p_return .= '<ul class="icon-folder">';
                        foreach ($terms as $term) {
                            $p_return .= '<li><a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a></li>' . "\n";
                        }
                        $p_return .= '</ul>';
                    }
                }

            }

            $p_return .= '</div>';

        endwhile;

        if ('1' == $sc_atts['masonry'] || 'yes' == $sc_atts['masonry']) {
            // Close .mass_content
            $p_return .= '</div>';
        }
        $p_return .= '</div>';

        if ( ('view_all' == $pagin_type) || ( $sc_paginated_count > 1) ){
            // VIEW ALL PAGINATION

            if ( isset($sc_atts['category']) && '' != $sc_atts['category'] ){

                $all_cats = explode( ',', $sc_atts['category'] );
                $cats = array();
                $neg_cas = array();

                foreach ( $all_cats as $cat_slug ){
                    $cat_slug = trim($cat_slug);

                    if ( '-' != $cat_slug[0] ){
                        // Positive categories
                        $cat = get_term_by( 'slug', $cat_slug, 'portfolio-category' );
                        if ($cat) $cats[] = $cat;
                    }
                }

                if ( count( $cats ) > 0 ){
                    $p_return .= '<div class="grid12 acenter"><a href="' . get_term_link( $cats[0] ) . '" class="ish-button-small color2">' . __('View all items', 'ishyoboy') . '</a></div>';
                }
            }
        }
        elseif ('' == $pagin_type){
            // NO PAGINATION
        }
        else{
            // NUMERIC PAGINATION
            if (isset($sc_atts['pagination']) && 'yes' == $sc_atts['pagination']){
                $p_return .= ishyoboy_get_pagination('', 3, $wpbp->max_num_pages, $wpbp->query['paged']);
            }
        }

    else :

        $p_return .='<div class="box error close"><a href="#alert-close" class="icon-cancel-1"></a>';
        $p_return .='    <p>' . __("Sorry, there's nothing to display in here.", 'ishyoboy') . '</p>';
        $p_return .='</div>';

    endif;

    $p_return .= '</div>';

    wp_reset_query();

    return $p_return;

}
}

// PORTFOLIO ONLY: IMAGES
if ( !function_exists('ishyoboy_shortcode_portfolio_images') ) {
function ishyoboy_shortcode_portfolio_images( $atts, $content = null ) {
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'slideshow' => 'yes',
        'autoslide' => '',
        'animation' => '',
        'interval' => '',
        'navigation' => '',
        'thumbnail_size' => 'theme-large',
        //'sameheight' => ''
    ), $atts);

    $return = '';

    $image_size = 'theme-large';
    if ( $sc_atts['thumbnail_size'] != '') { $image_size = $sc_atts['thumbnail_size']; }
    $id = get_the_ID();
    $wp_3_5 = function_exists( 'wp_enqueue_media' );

    if ( $wp_3_5 ){
        $images = array_filter( explode( ',', IshYoMetaBox::get('ishyoboy_porfolio_images', true, $id) ) );
    }
    else{
        $images = get_children(array(
                'post_parent' => $id,
                'post_type' => 'attachment',
                'numberposts' => -1,
                'post_mime_type' => 'image',
            )
        );
    }

    if($images) {

        if ('yes' == $sc_atts['slideshow']){

	        wp_enqueue_script( 'imagesloaded' );
            wp_enqueue_script( 'flexslider' );
	        //wp_enqueue_script( 'isotope' );

            $return .= '[slidable';

            $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
            $return .= ('' != $sc_atts['css_class']) ? ' class="portfolio_images_slideshow ' . esc_attr($sc_atts['css_class']) .'"' : ' class="portfolio_images_slideshow"' ;
            $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : '' ;
            $return .= ('' != $sc_atts['tooltip']) ? ' tooltip="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
            $return .= ('' != $sc_atts['tooltip_color']) ? ' tooltip_color="' . esc_attr($sc_atts['tooltip_color']) . '"' : ''  ;
            $return .= ('' != $sc_atts['autoslide']) ? ' autoslide="' . esc_attr($sc_atts['autoslide']) .'"' : '' ;
            $return .= ('' != $sc_atts['animation']) ? ' animation="' . esc_attr($sc_atts['animation']) .'"' : '' ;
            $return .= ('' != $sc_atts['interval']) ? ' interval="' . esc_attr($sc_atts['interval']) .'"' : '' ;
            $return .= ('' != $sc_atts['navigation']) ? ' navigation="' . esc_attr($sc_atts['navigation']) .'"' : '' ;
            //$return .= ('' != $sc_atts['sameheight']) ? ' sameheight="' . esc_attr($sc_atts['sameheight']) .'"' : '' ;

            $return .= ']';

            if ( $wp_3_5 ){
                if ( $images ){
                    foreach ( $images as $attachment_id ) {
                        $return .= '[slide class="portfolio_slide"]';
                        $return .= '<a href="' . wp_get_attachment_url($attachment_id) . '" rel="portfolio-gallery-' . $id . '" class="openfancybox-image">';
                        $return .=  wp_get_attachment_image( $attachment_id, $image_size );
                        $return .= '</a>';
                        $return .= '[/slide]';
                    }
                }
            }
            else{
                foreach( $images as $image ) {
                    $return .= '[slide class="portfolio_slide"]';
                    $return .= '<a href="' . wp_get_attachment_url($image->ID) . '" rel="portfolio-gallery-' . $id . '" class="openfancybox-image">';
                    $return .= wp_get_attachment_image($image->ID, $image_size);
                    $return .= '</a>';
                    $return .= '[/slide]';
                }
            }

            $return .= '[/slidable]';

        }
        else{
            $return .= '<div class="portfolio_images';
            $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
            $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
            $return .= '"' ;
            $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
            $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
            $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
            $return .= '>' . "\n";

            if ( $wp_3_5 ){
                if ( $images ){
                    foreach ( $images as $attachment_id ) {
                        $return .= '<div class="portfolio_image">';
                        $return .= '<a href="' . wp_get_attachment_url($attachment_id) . '" rel="portfolio-gallery-' . $id . '" class="openfancybox-image">';
                        $return .= wp_get_attachment_image( $attachment_id, $image_size );
                        $return .= '</a>';
                        $return .= '</div>';
                    }
                }
            }
            else{
                if ( $images ){
                    foreach( $images as $image ) {
                        $return .= '<div class="portfolio_image">';
                        $return .= '<a href="' . wp_get_attachment_url($image->ID) . '" rel="portfolio-gallery-' . $id . '" class="openfancybox-image">';
                        $return .= wp_get_attachment_image($image->ID, $image_size);
                        $return .= '</a>';
                        $return .= '</div>';
                    }
                }
            }

            $return .= '</div>';
        }

        return do_shortcode($return);
    }

}
}

// PORTFOLIO ONLY: CATEGORIES
if ( !function_exists('ishyoboy_shortcode_portfolio_categories') ) {
function ishyoboy_shortcode_portfolio_categories( $atts, $content = null ) {
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'links' => 'yes',
        'separator' => ', ',
        'before' => '',
        'after' => '',
        'container' => 'p'
    ), $atts);

    $return = '';

    $terms = get_the_terms(  get_the_ID(), 'portfolio-category' );

    if (isset($terms) && '' != $terms ) {

        $return .= '<' . $sc_atts['container'] . ' class="portfolio-categories-list';

        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
        $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
        $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        $sc_atts['before'] = html_entity_decode( $sc_atts['before'] );
        $sc_atts['after'] = html_entity_decode( $sc_atts['after'] );

        $i = 0;
        $count = count($terms);
        foreach ($terms as $term) {
            $i++;
            if ( 'yes' == $sc_atts['links'] ){
                $return .= $sc_atts['before'] . '<a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a>' . $sc_atts['after'];
            }
            else{
                $return .= $sc_atts['before'] . $term->name . $sc_atts['after'];
            }

            if( $i != $count ) {
                $return .= $sc_atts['separator'];
            }

        }

        $return .= '</' . $sc_atts['container'] . '>';
    }

    return $return;

}
}

// PORTFOLIO PREV NEXT
if ( !function_exists('ishyoboy_shortcode_portfolio_prev_next') ) {
function ishyoboy_shortcode_portfolio_prev_next( $atts, $content = null ) {
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'prev_text' => '',
        'next_text' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="portfolio-prev-next';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>' . "\n";

    $nav_next = get_permalink( get_adjacent_post( false, '', false ) );
    $nav_prev = get_permalink( get_adjacent_post( false, '', true ) );

    if ( get_permalink() != $nav_next ){
        $return .= '<div class="portfolio-next-link"><a class="ish-button-small' . (('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '') . '" href="' . esc_attr($nav_next) . '">' . (('' != $sc_atts['next_text']) ? $sc_atts['next_text'] : '&lt; Newer project') . '</a></div>';
    }

    if ( get_permalink() != $nav_prev ){
        $return .= '<div class="portfolio-prev-link"><a class="ish-button-small' . (('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '') . '" href="' . esc_attr($nav_prev) . '">' . (('' != $sc_atts['prev_text']) ? $sc_atts['prev_text'] : 'Older project &gt;') . '</a></div>';
    }

    $return .= '</div>';

    return do_shortcode($return);

}
}

// RECENT POSTS
if ( !function_exists('ishyoboy_shortcode_recent_posts') ) {
function ishyoboy_shortcode_recent_posts($atts, $content=null){

    global $ish_options;

    $posts_per_page = get_option('posts_per_page');

    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'category' => '',
        'order' => 'DESC',
        'columns' => '4',
        'count' => '4',
        'show_title_icon' => 'no',
        'show_media' => 'yes',
        'show_date' => 'yes',
        'show_categories' => 'yes',
        'show_read_more' => 'yes',

        'show_author' => 'no',
        'show_tags' => 'no',
        'show_comments' => 'no',

        'slideshow' => 'no',
        'autoslide' => '', //"yes" or "no"
        'animation' => '', // "slide" or "fade"
        'interval' => '', // "slide" or "fade"
        'navigation' => '', // "slide" or "fade"


        'skip' => '', // Number of posts to skip
        'order_by' => '', // Change the ordering criteria, default: 'date' (http://codex.wordpress.org/Class_Reference/WP_Query#Order_.26_Orderby_Parameters)
        'show_excerpt' => 'yes',
        'post_format' => '', // Filter results by comma separated post formats (positive and negative)
        'post_ids' => '', // Filter results by comma separated post IDs (positive and negative)
        'post_tags' => '', // Filter results by comma separated post Tags (positive and negative)
        // 'category' - Negative categories added, multiple categories added

    ), $atts);

    $return = '';

    // Default type of connecting multiple taxonomy "filters". Do not change this!
    $tax_query = array(
        'relation' => 'AND'
    );


    // Filter by category
    if (isset($sc_atts['category']) && '' != $sc_atts['category']){
        // Display items from a chosen category

        $cats = explode( ',', $sc_atts['category'] );
        $cat_ids = array();
        $neg_cat_ids = array();

        foreach ( $cats as $cat_slug ){
            $cat_slug = trim($cat_slug);

            if ( '-' == $cat_slug[0] ){
                // Negative categories = exclude those
                $cat = get_term_by( 'slug', $cat_slug, 'category' );
                if ($cat) $neg_cat_ids[] = $cat->term_id;
            }
            else{
                // Positive categories
                $cat = get_term_by( 'slug', $cat_slug, 'category' );
                if ($cat) $cat_ids[] = $cat->term_id;
            }
        }



        if ( count( $cat_ids ) > 0 || count( $neg_cat_ids ) > 0 ){

            if ( count( $cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'category',
                    'field' => 'id',
                    'terms' => $cat_ids
                );
            }

            if ( count( $neg_cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'category',
                    'field' => 'id',
                    'terms' => $neg_cat_ids,
                    'operator' => 'NOT IN'
                );
            }

        }
        else {

            $tax_query[] = array(
                'taxonomy' => 'category',
                'field' => 'slug',
                'terms' => $cats
            );

        }

    }
    else{
        // Display items from all categories
        //$tax_query = array();
    }


    // Filter by post format
    if ( isset($sc_atts['post_format']) && '' != $sc_atts['post_format']){
        // Display items from a chosen category

        if ( 'standard' == $sc_atts['post_format'] ){$sc_atts['post_format'] = '-quote,-video,-audio,-link,-aside,-image,-chat,-gallery,-status';}

        $cats = explode( ',', $sc_atts['post_format'] );
        $cat_ids = array();
        $neg_cat_ids = array();

        foreach ( $cats as $cat_slug ){
            $cat_slug = trim($cat_slug);

            if ( '-' == $cat_slug[0] ){
                // Negative categories = exclude those
                $cat = get_term_by( 'slug', 'post-format-' . $cat_slug, 'post_format' );
                if ($cat) $neg_cat_ids[] = $cat->term_id;
            }
            else{
                // Positive categories
                $cat = get_term_by( 'slug', 'post-format-' . $cat_slug, 'post_format' );
                if ($cat) $cat_ids[] = $cat->term_id;
            }
        }



        if ( count( $cat_ids ) > 0 || count( $neg_cat_ids ) > 0 ){

            if ( count( $cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'post_format',
                    'field' => 'id',
                    'terms' => $cat_ids
                );
            }

            if ( count( $neg_cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'post_format',
                    'field' => 'id',
                    'terms' => $neg_cat_ids,
                    'operator' => 'NOT IN'
                );
            }

        }
        else {

            $tax_query[] = array(
                'taxonomy' => 'post_format',
                'field' => 'slug',
                'terms' => $cats
            );

        }

    }
    else{
        // Display items from all categories
        //$tax_query = array();
    }

    // Filter by tags
    if (isset($sc_atts['post_tags']) && '' != $sc_atts['post_tags']){
        // Display items from a chosen category

        $cats = explode( ',', $sc_atts['post_tags'] );
        $cat_ids = array();
        $neg_cat_ids = array();

        foreach ( $cats as $cat_slug ){
            $cat_slug = trim($cat_slug);

            if ( '-' == $cat_slug[0] ){
                // Negative categories = exclude those
                $cat = get_term_by( 'slug', $cat_slug, 'post_tag' );
                if ($cat) $neg_cat_ids[] = $cat->term_id;
            }
            else{
                // Positive categories
                $cat = get_term_by( 'slug', $cat_slug, 'post_tag' );
                if ($cat) $cat_ids[] = $cat->term_id;
            }
        }



        if ( count( $cat_ids ) > 0 || count( $neg_cat_ids ) > 0 ){

            if ( count( $cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'post_tag',
                    'field' => 'id',
                    'terms' => $cat_ids
                );
            }

            if ( count( $neg_cat_ids ) > 0 ) {
                $tax_query[] = array(
                    'taxonomy' => 'post_tag',
                    'field' => 'id',
                    'terms' => $neg_cat_ids,
                    'operator' => 'NOT IN'
                );
            }

        }
        else {

            $tax_query[] = array(
                'taxonomy' => 'post_tag',
                'field' => 'slug',
                'terms' => $cats
            );

        }

    }
    else{
        // Display items from all categories
        //$tax_query = array();
    }

    $positive_ids = array();
    $negative_ids = array();

    // Filter by Post IDs
    if (isset($sc_atts['post_ids']) && '' != $sc_atts['post_ids']){
        // Display items from a chosen category

        $cats = explode( ',', $sc_atts['post_ids'] );

        foreach ( $cats as $cat_slug ){
            $cat_slug = trim($cat_slug);

            if ( '-' == $cat_slug[0] ){
                // Negative ids = exclude those
                $negative_ids[] = $cat_slug;//substr( $cat_slug, 1);
            }
            else{
                // Positive ids
                $positive_ids[] = $cat_slug;
            }
        }
    }

    $thumb_size = 'theme-large';

    if (is_numeric($sc_atts['columns'])){
        switch ($sc_atts['columns']) {
            case '4' :
                $grid = 'grid3';
                $thumb_size = 'theme-fourth';
                break;
            case '3' :
                $grid = 'grid4';
                $thumb_size = 'theme-third';
                break;
            case '2' :
                $grid = 'grid6';
                $thumb_size = 'theme-half';
                break;
            case '1' :
                $grid = 'grid12';
                $thumb_size = 'theme-full';
                break;
            case '5' :
                $grid = 'grid2';
                $thumb_size = 'theme-fourth';
                break;
            case '6' :
                $grid = 'grid2';
                $thumb_size = 'theme-fourth';
                break;
            default :
                $grid = 'grid3';
                $thumb_size = 'theme-fourth';
        }
    }
    $order = ('ASC' == strtoupper($sc_atts['order'])) ? 'ASC' : 'DESC' ; // ASC OR DESC
    $orderby = ( isset( $sc_atts['order_by'] ) && '' != $sc_atts['order_by'] ) ? $sc_atts['order_by'] : 'date';

    $offset = (isset($sc_atts['skip']) && is_numeric( $sc_atts['skip'] ) ) ? $sc_atts['skip'] : '0';

    // Get all Portfolio posts
    $wpbp = new WP_Query( array(
            'post_type' =>  'post',
            'posts_per_page'  => $sc_atts['count'],
            'order' => $order,
            'orderby' => $orderby,
            'offset' => $offset,
            'post__in' => $positive_ids,
            'post__not_in' => $negative_ids,
            'post_status' => 'publish',
            'tax_query' => $tax_query
        )
    );

    if ($wpbp->have_posts()) {


        if ('yes' == $sc_atts['slideshow']){

	        wp_enqueue_script( 'imagesloaded' );
	        wp_enqueue_script( 'flexslider' );
	        //wp_enqueue_script( 'isotope' );

            $return .= '[slidable';

            $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
            $return .= ('' != $sc_atts['css_class']) ? ' class="recent_posts recent_posts_slideshow ' . esc_attr($sc_atts['css_class']) .'"' : ' class="recent_posts recent_posts_slideshow"' ;
            $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : '' ;
            $return .= ('' != $sc_atts['tooltip']) ? ' tooltip="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
            $return .= ('' != $sc_atts['tooltip_color']) ? ' tooltip_color="' . esc_attr($sc_atts['tooltip_color']) . '"' : ''  ;
            $return .= ('' != $sc_atts['align']) ? ' align="' . esc_attr($sc_atts['align']) .'"' : '' ;
            $return .= ('' != $sc_atts['autoslide']) ? ' autoslide="' . esc_attr($sc_atts['autoslide']) .'"' : '' ;
            $return .= ('' != $sc_atts['animation']) ? ' animation="' . esc_attr($sc_atts['animation']) .'"' : '' ;
            $return .= ('' != $sc_atts['interval']) ? ' interval="' . esc_attr($sc_atts['interval']) .'"' : '' ;
            $return .= ('' != $sc_atts['navigation']) ? ' navigation="' . esc_attr($sc_atts['navigation']) .'"' : '' ;
            //$return .= ('' != $sc_atts['sameheight']) ? ' sameheight="' . esc_attr($sc_atts['sameheight']) .'"' : '' ;

            $return .= ']';

            $return .= '[slide class="recent_posts_slide"]';
        }
        else{
            $return .= '<div class="row">';
        }

        $count = 0;

        while ($wpbp->have_posts()) {
            $wpbp->the_post();
            $count++;
            $terms = get_the_terms(  get_the_ID(), 'category' );

            $return .= '<div class="recent_posts_post ' . $grid . '">';

            $format = get_post_format();

            switch ($format){
                case 'video':
                    $post_icon = 'icon-video';
                    break;
                case 'audio':
                    $post_icon = 'icon-music-1';
                    break;
                case 'link':
                    $post_icon = 'icon-link';
                    break;
                case 'quote':
                    $post_icon = 'icon-quote';
                    break;
                default:
                    $post_icon = 'icon-align-center';
            }

            $return .= '<div class="recent_posts_post_content">';

            // Blog title
            if ( 'yes' == $sc_atts['show_title_icon']){
                $return .= '<h3 class="' . $post_icon . ' color2">' . '<a href="' . get_permalink() . '">' . get_the_title() . '</a>' . '</h3>';
            }
            else{
                $return .= '<h3 class="color2">' . '<a href="' . get_permalink() . '">' . get_the_title() . '</a>' . '</h3>';
            }

            // Blog post media
            switch ($format){

                case 'video':
                    if ( 'yes' == $sc_atts['show_media']){
                        ob_start();
                        ishyoboy_the_post_video(get_the_ID());
                        $doc = ob_get_clean();
                        $return .= str_replace(array("\r\n", "\n", "\r"), '', $doc);
                    }
                    break;

                case 'audio':
                    if ( 'yes' == $sc_atts['show_media']){
                        ob_start();
                        ishyoboy_the_post_audio(get_the_ID());
                        $doc = ob_get_clean();
                        $return .= str_replace(array("\r\n", "\n", "\r"), '', $doc);
                    }
                    break;

                case 'link':
                    if ( 'yes' == $sc_atts['show_excerpt']){
                        $return .= '<a href="'.  esc_attr( ishyoboy_get_post_format_url() ) . '" target="_blank" class="ish-button-big color3 pt-link">' . get_the_content() . '</a>';
                    }
                    break;

                case 'quote':
                    if ( 'yes' == $sc_atts['show_excerpt']){
                        ob_start();
                        $quote = ishyoboy_get_post_format_quote();
                        if ('' != $quote){
                            echo '<blockquote class="color2 post-quote-content">';
                            echo $quote;

                            // Get Quote source
                            $quote_source = ishyoboy_get_post_format_quote_source();
                            if ('' != $quote_source){

                                // Get Quote URL
                                $quote_url = ishyoboy_get_post_format_url();
                                if ('' != $quote_url){

                                    echo '<cite><a href="', $quote_url ,'" target="_blank">', $quote_source, '</a></cite>';

                                }
                                else{

                                    echo '<cite>', $quote_source, '</cite>';

                                }

                            }

                            echo '</blockquote>';
                        }
                        $return .= ob_get_clean();
                    }
                    break;

                default:
                    if ( 'yes' == $sc_atts['show_media']){
                        if ( (function_exists('has_post_thumbnail')) && (has_post_thumbnail()) ) {
                            $return .= '<div class="main-post-image"><a href="' . get_permalink() . '">' . get_the_post_thumbnail(get_the_ID(), $thumb_size) . '</a></div>';
                        }
                    }
            }

            // Excerpt
            if ( 'yes' == $sc_atts['show_excerpt']){
                if ( 'link' != $format ){
                    $post = get_post();
                    $excerpt = '';
                    $excerpt = ishyoboy_custom_excerpt($post->post_content, 30);

                    if ( '' != $excerpt ){
                        $return .=  "\n\n" . '<div class="excerpt"><p>' . $excerpt . '</p></div>' . "\n\n";
                    }
                }
            }

            // Blog post details
            $return .= '<div class="blog-post-details">';

            if ( 'yes' == $sc_atts['show_date'] ) {
                $return .= '<span class="icon-calendar">' . get_the_date() . '</span>';
            }

            if ( 'yes' == $sc_atts['show_author'] ) {
                $return .= '<span class="icon-pencil-1">' . get_the_author() . '</span>';
            }

            if ( 'yes' == $sc_atts['show_categories'] ) {
                if ( has_category() ) {
                    $return .= '<span class="icon-folder">';

                    if (isset($terms) && '' != $terms ) {
                        $i = 0;
                        foreach ($terms as $term) {
                            $i++;
                            $return .= '<a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a>' . " ";
                            if (3 == $i ) {
                                if ( count($terms) > $i ) { $return .= '...'; }
                                break;
                            }
                        }
                    }

                    $return .= '</span>';
                }
            }

            if ( 'yes' == $sc_atts['show_tags'] ) {
                if ( has_tag() ) {
                    $terms = get_the_tags();
                    $return .= '<span class="icon-tags">';

                    if (isset($terms) && '' != $terms ) {
                        $i = 0;
                        foreach ($terms as $term) {
                            $i++;
                            $return .= '<a href="' . esc_attr(get_term_link($term)) . '">' . $term->name . '</a>' . " ";
                            if (3 == $i ) {
                                if ( count($terms) > $i ) { $return .= '...'; }
                                break;
                            }
                        }
                    }

                    $return .= '</span>';
                }
            }

            if ( 'yes' == $sc_atts['show_comments'] ) {
                $num_comments = get_comments_number(); // get_comments_number returns only a numeric value

                if ( comments_open() ) {
                    $return .= '<span class="icon-chat-1">';
                    if ( $num_comments == 0 ) {
                        //$comments = __('0 comments', 'ishyoboy');
                        $comments = 0;
                    } elseif ( $num_comments > 1 ) {
                        //$comments = $num_comments . __(' comments', 'ishyoboy');
                        $comments = $num_comments;
                    } else {
                        //$comments = __('1 comment', 'ishyoboy');
                        $comments = 1;
                    }
                    $return .= '<a href="' . get_comments_link() .'">'. $comments.'</a>';
                    $return .= '</a></span>';
                } else {
                    //$return .=   __('Comments are off for this post.');
                }
            }

            $return .= '</div>';

            // Read more link
            if ('yes' == $sc_atts['show_read_more']){
                $return .=  '<a class="color1 right" href="' . get_permalink() . '">' . __( 'Read more', 'ishyoboy') . '</a>';
            }
            $return .= '</div></div>';

            if ( ( $count % $sc_atts['columns'] == 0 ) && ( $count != $sc_atts['count'] ) && ( $count != $wpbp->post_count ) ) {

                if ( 'yes' == $sc_atts['slideshow'] ){
                    $return .= '[/slide]
                    [slide class="recent_posts_slide"]';
                }
                else{
                    $return .= '</div><div class="space"></div><div class="row">';
                }
            }

        } // Endwhile

        if ( 'yes' == $sc_atts['slideshow'] ){
            $return .= '[/slide][/slidable]';
        }
        else{
            $return .= '</div>';
        }
    }

    wp_reset_query();

    return do_shortcode($return);
}
}

// BREADCRUMBS
if ( !function_exists('ishyoboy_shortcode_breadcrumbs') ) {
function ishyoboy_shortcode_breadcrumbs( $atts, $content = null ) {

    global $ish_options, $ish_woo_id;

    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'color' => '',
        'align' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="list-breadcrumbs';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>' . "\n";

    if ( !is_front_page() ) {

        if ( function_exists('is_woocommerce') ) {
            if ( !is_woocommerce() && !is_woocommerce_page() ){
                //$return .= '<p class="breadcrumb"><span class="breadcrumb_info">You are here:</span> <a href="';
                $return .= '<p class="breadcrumb"><a href="';
                $return .= home_url();
                $return .= '">';
                $return .= __('Home', 'ishyoboy');
                $return .= "</a> &gt; ";
            }

        }
        else{
            //$return .= '<p class="breadcrumb"><span class="breadcrumb_info">You are here:</span> <a href="';
            $return .= '<p class="breadcrumb"><a href="';
            $return .= home_url();
            $return .= '">';
            $return .= __('Home', 'ishyoboy');
            $return .= "</a> &gt; ";
        }

    }

    if ( (is_category() || is_single()) && ( !function_exists('is_woocommerce_page') || ( !is_woocommerce() && !is_woocommerce_page() ) ) ) {

        $post_id = get_the_ID();
        $post_type = get_post_type();

        switch ($post_type){
            case 'portfolio-post' :
                $terms = get_the_terms($post_id, 'portfolio-category' );
                $term = array_pop($terms);

                if (isset($ish_options['page_for_custom_post_type_portfolio-post']) && '-1' != $ish_options['page_for_custom_post_type_portfolio-post']){

                    $portfolio_page = get_page($ish_options['page_for_custom_post_type_portfolio-post']);
                    $separator = " &gt; ";
                    $return .= '<a href="' . get_page_link( $ish_options['page_for_custom_post_type_portfolio-post'] ) . '" title="' . esc_attr( sprintf( __( "View all posts in %s" , 'ishyoboy' ), $portfolio_page->post_title ) ) . '">'.$portfolio_page->post_title.'</a>' . $separator;
                }

                $return .= get_term_parents($term->term_id, 'portfolio-category', TRUE, ' &gt; ', FALSE );
                break;
            default :
                $category = get_the_category();
				if ( ! empty( $category ) ){
	                $ID = $category[0]->cat_ID;
	                $return .= get_category_parents($ID, TRUE, ' &gt; ', FALSE );
				}
        }
    } else if ( function_exists('is_woocommerce_page') && (is_woocommerce() || is_woocommerce_page() )){
        ob_start();
        woocommerce_breadcrumb(array(
            'delimiter'   => ' &gt; ',
            'wrap_before' => '<p class="breadcrumb">',
            'wrap_after'  => '</p>',
            'before'      => '',
            'after'       => '',
            'home'        => _x( 'Home', 'breadcrumb', 'woocommerce' ),
        ));
        $return .= ob_get_contents();
        ob_end_clean();
    }
    else if (is_tax()){
        if (is_tax('portfolio-category')){

            $current_term = get_queried_object();

            if ( !empty($current_term) ){
                //var_dump($current_term);

                if (isset($ish_options['page_for_custom_post_type_portfolio-post']) && '-1' != $ish_options['page_for_custom_post_type_portfolio-post']){

                    $portfolio_page = get_page($ish_options['page_for_custom_post_type_portfolio-post']);
                    $separator = " &gt; ";
                    $return .= '<a href="' . get_page_link( $ish_options['page_for_custom_post_type_portfolio-post'] ) . '" title="' . esc_attr( sprintf( __( "View all posts in %s" , 'ishyoboy' ), $portfolio_page->post_title ) ) . '">'.$portfolio_page->post_title.'</a>' . $separator;
                }

                if ($current_term->parent != 0 ){
                    $return .= get_term_parents($current_term->parent, 'portfolio-category', TRUE, ' &gt; ', FALSE );
                }

                $return .= $current_term->name;
            }
        }
    }
    else if (is_page()){
        global $post;
        if ($post->post_parent != 0 ){
            $return .= get_page_parents($post->post_parent, TRUE, ' &gt; ', FALSE );
        }

    }

    if (!function_exists('is_woocommerce_page') || !is_woocommerce_page()){
        if(is_single() || is_page()) {$return .= get_the_title();}
        if(is_tag()){ $return .= "Tag: " . single_tag_title('',FALSE); }
        if(is_404()){ $return .= "404 - Page not Found"; }
        if(is_search()){ $return .= "Search"; }
        if(is_year()){ $return .= get_the_time('Y'); };
    }

    $return .= '</div>';

    return $return;
}
}

// MARK
if ( !function_exists('ishyoboy_shortcode_mark') ) {
function ishyoboy_shortcode_mark($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => ''
    ), $atts);

    $return = '';

    $return .= '<mark class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</mark>';

    return $return ;
}
}

// TOOLTIP
if ( !function_exists('ishyoboy_shortcode_tooltip') ) {
function ishyoboy_shortcode_tooltip($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => ''
    ), $atts);

    $return = '';
    $return .= '<span class="tooltip-text';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</span>';

    return $return ;
}
}

// SKILLS
if ( !function_exists('ishyoboy_shortcode_skills') ) {
function ishyoboy_shortcode_skills($atts, $content = null){
    global $skills_outside;

    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="list-skills';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// SKILL
if ( !function_exists('ishyoboy_shortcode_skill') ) {
function ishyoboy_shortcode_skill($atts, $content = null){
    global $skills_outside;

    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'percent' => '100',
        'outside' => 'no'
    ), $atts);

    $return = '';

    $return .= '<div>';
    if ( 'yes' == $sc_atts['outside'] ){
        $return .= '<span>' . do_shortcode( $content ) . '</span>';
    }
	$return .= '<div class="';
	$return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) . '"' : '"' ;
	$return .= '>';
	$return .= '<span class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['outside']) ? ' outside' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= ('' != $sc_atts['percent']) ? ' data-count="' . esc_attr($sc_atts['percent']) . '"' : ''  ;
    $return .= '>';

    if ('yes' != $sc_atts['outside']){
        $return .= do_shortcode( $content );
    }

    $return .= '</span></div></div>';

    return $return ;
}
}

// DROPCAP
if ( !function_exists('ishyoboy_shortcode_dropcap') ) {
function ishyoboy_shortcode_dropcap($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'boxed' => 'no'
    ), $atts);

    $return = '';

    $return .= '<span class="dropcap';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['boxed']) ? ' bg-dropcap' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</span>';

    return $return ;
}
}

// ACCORDION
if ( !function_exists('ishyoboy_shortcode_accordion') ) {
function ishyoboy_shortcode_accordion($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'tag' => 'h5'
    ), $atts);

    $return = '';

    $return .= '<ul class="accordion';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= str_replace('###_IYB_TAG_###', esc_attr($sc_atts['tag']) , do_shortcode( $content )) . '</ul>';

    return $return ;
}
}

// ACCORDION ITEM
if ( !function_exists('ishyoboy_shortcode_acc_item') ) {
function ishyoboy_shortcode_acc_item($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'title' => '',
        'tag' => '###_IYB_TAG_###',
        'active' => ''
    ), $atts);

    $return = '';

    $return .= '<li class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= ('yes' == $sc_atts['active']) ? ' active' : '' ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    if ('' != $sc_atts['title']){
        $return .= '<' . $sc_atts['tag'] . ' class="acc-title"' . '>' . $sc_atts['title'] . '</' . $sc_atts['tag'] . '>';
    }

    $return .= '<div class="acc-content">' . do_shortcode( $content ) . '</div></li>';

    return $return ;
}
}

// TOGGLE
if ( !function_exists('ishyoboy_shortcode_toggle') ) {
function ishyoboy_shortcode_toggle($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'tag' => 'h5'
    ), $atts);

    $return = '';

    $return .= '<ul class="toggle';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= str_replace('###_IYB_TAG_###', esc_attr($sc_atts['tag']) , do_shortcode( $content )) . '</ul>';

    return $return ;
}
}

// TOGGLE ITEM
if ( !function_exists('ishyoboy_shortcode_tgg_item') ) {
function ishyoboy_shortcode_tgg_item($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'title' => '',
        'tag' => '###_IYB_TAG_###',
        'active' => ''
    ), $atts);

    $return = '';

    $return .= '<li class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= ('yes' == $sc_atts['active']) ? ' active' : '' ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    if ('' != $sc_atts['title']){
        $return .= '<' . $sc_atts['tag'] . ' class="tgg-title"' . '>' . $sc_atts['title'] . '</' . $sc_atts['tag'] . '>';
    }

    $return .= '<div class="tgg-content">' . do_shortcode( $content ) . '</div></li>';

    return $return ;
}
}

// PRE
if ( !function_exists('ishyoboy_shortcode_pre') ) {
function ishyoboy_shortcode_pre($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
    ), $atts);

    $return = '';

    $return .= '<pre class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= $content . '</pre>';

    return $return ;
}
}

// CODE
if ( !function_exists('ishyoboy_shortcode_code') ) {
function ishyoboy_shortcode_code($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
    ), $atts);

    $return = '';

    $return .= '<code class="';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</code>';

    return $return ;
}
}

// TABS NAVIGATION
if ( !function_exists('ishyoboy_shortcode_tabs_navigation') ) {
function ishyoboy_shortcode_tabs_navigation($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'pair' => null,
        'vertical' => 'no',
        'color' => ''
    ), $atts);

    $return = '';

    if ( null != $sc_atts['pair']){

        $return .= '<ul class="tabs-navigation';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= ('yes' == $sc_atts['vertical']) ? ' vertical' : '' ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= ('' != $sc_atts['pair']) ? ' data-tabs="' . esc_attr($sc_atts['pair']) . '"' : ''  ;
        $return .= '>';
        $return .= do_shortcode( $content ) . '</ul>';

    }else{
        $return .='<div class="box error close"><a href="#alert-close" class="icon-cancel-1"></a>';
        $return .='    <p>' . __('Please set the same "pair" parameter both to [tabs_navigation] and [tabs_content] to link them together.', 'ishyoboy') . '</p>';
        $return .='</div>';
    }

    return $return ;
}
}

// TABS CONTENT
if ( !function_exists('ishyoboy_shortcode_tabs_content') ) {
function ishyoboy_shortcode_tabs_content($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'pair' => null,
    ), $atts);

    $return = '';

    if ( null != $sc_atts['pair']){

        $return .= '<div class="tabs-content';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= ('' != $sc_atts['pair']) ? ' data-tabs="' . esc_attr($sc_atts['pair']) . '"' : ''  ;
        $return .= '>';
        $return .= do_shortcode( $content ) . '</div>';

    }else{
        $return .='<div class="box error close"><a href="#alert-close" class="icon-cancel-1"></a>';
        $return .='    <p>' . __('Please set the same "pair" parameter both to [tabs_navigation] and [tabs_content] to link them together.', 'ishyoboy') . '</p>';
        $return .='</div>';
    }

    return $return ;
}
}

// TAB TITLE
if ( !function_exists('ishyoboy_shortcode_tab_title') ) {
function ishyoboy_shortcode_tab_title($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'pair' => null,
        'active' => ''
    ), $atts);

    $return = '';

    if ( null != $sc_atts['pair'] ){

        $return .= '<li class="';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= ('yes' == $sc_atts['active']) ? ' active' : '' ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        $return .= '<a href="#' . esc_attr($sc_atts['pair']) . '">' . do_shortcode( $content ) . '</a></li>';
    }
    else{
        $return .='<div class="box error close"><a href="#alert-close" class="icon-cancel-1"></a>';
        $return .='    <p>' . __('Please set the same "pair" parameter both to [tab_title] and [tab_content] to link them together.', 'ishyoboy') . '</p>';
        $return .='</div>';
    }

    return $return ;
}
}

// TAB CONTENT
if ( !function_exists('ishyoboy_shortcode_tab_content') ) {
function ishyoboy_shortcode_tab_content($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'pair' => null,
        'active' => ''
    ), $atts);

    $return = '';

    if ( null != $sc_atts['pair'] ){

        $return .= '<div class="';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('' != $sc_atts['pair']) ? ' ' . esc_attr($sc_atts['pair']) : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= ('yes' == $sc_atts['active']) ? ' active' : '' ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        $return .= do_shortcode( $content ) . '</div>';
    }
    else{
        $return .='<div class="box error close"><a href="#alert-close" class="icon-cancel-1"></a>';
        $return .='    <p>' . __('Please set the same "pair" parameter both to [tab_title] and [tab_content] to link them together.', 'ishyoboy') . '</p>';
        $return .='</div>';
    }

    return $return ;
}
}
// CONTENT BREAK
if ( !function_exists('ishyoboy_shortcode_content_break') ) {
function ishyoboy_shortcode_content_break($atts, $content = null){

    $sidebar_position = ishyoboy_get_sidebar_position();

    if ( ('left' != $sidebar_position && 'right' != $sidebar_position ) || is_home() ){
        return '</div></div></section>' . do_shortcode( $content ) . '<section class="part-content"><div class="row"><div class="grid12 no-sidebar">';
    }
    else{
        return '';
        //do_shortcode('[alert type="error" closable="yes"]Sorry, [[content_break]] and [[section]] can not be used on a page with a sidebar.[/alert]');
    }
}
}

// PAGE BREAK
if ( !function_exists('ishyoboy_shortcode_page_break') ) {
function ishyoboy_shortcode_page_break($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'full_width' => 'no'
    ), $atts);

    $sidebar_position = ishyoboy_get_sidebar_position();

    if ( ('left' != $sidebar_position && 'right' != $sidebar_position) || is_home() ){
        $return = '';

        $return .= '</div></div></section></div>';

        $return .= '<div class="part-pagebreak';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('yes' == $sc_atts['full_width']) ? ' full-width' : '' ;
        $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
        $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
        $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        $return .= '<div class="row">' . do_shortcode( $content ) . '</div></div><div class="wrapper-all"><section class="part-content"><div class="row"><div class="grid12 no-sidebar">';

        return $return;
    }
    else{
        return '';
        //do_shortcode('[alert type="error" closable="yes"]Sorry, [[page_break]] can not be used on a page with sidebar.[/alert]');
    }
}
}

// SECTION
if ( !function_exists('ishyoboy_shortcode_section') ) {
function ishyoboy_shortcode_section($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'full_width' => 'no',
        'pattern' => 'yes',
        'pattern_url' => '',
	    'parallax_type' => '',
	    'parallax_scroll' => '',
	    'parallax_duration' => '',
	    'parallax_easing' => '',
    ), $atts);

    $return = '';

    $return .= '<div class="part-fullsection';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' bg-' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['pattern'] && '' == $sc_atts['pattern_url'] ) ? ' bg-pattern' : '' ;
    $return .= ('yes' == $sc_atts['full_width']) ? ' full-width' : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' acenter' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' aright' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' aleft' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;

	// Parallax dynamic
	if ( 'yes' == $sc_atts['pattern'] && 'dynamic' == $sc_atts['parallax_type'] ) {
		wp_enqueue_script( 'backgroundpos' );
		wp_enqueue_script( 'parallax' );
		wp_enqueue_script( 'easing' );

		$return .= ' bgscroll' ;
	}

    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';

	if ( '' != $sc_atts['style'] || ( '' != $sc_atts['pattern_url'] && 'yes' == $sc_atts['pattern'] ) ){
		$return .= ' style="';

		$parallax = '';
		if ( 'yes' == $sc_atts['pattern'] && 'static' == $sc_atts['parallax_type'] ) {
			$parallax = 'background-position: top center; background-attachment: fixed;';
		}

		if ( 'yes' == $sc_atts['pattern'] && 'dynamic' == $sc_atts['parallax_type'] ) {
			$parallax = 'background-position: 50% 0; background-attachment: fixed;';
		}

		$return .= ( 'yes' == $sc_atts['pattern'] && '' != $sc_atts['pattern_url'] ) ? ' background-image: url(\'' . esc_attr( $sc_atts['pattern_url'] ) . '\'); ' . $parallax : '' ;

		if ( 'yes' == $sc_atts['pattern'] && 'static' == $sc_atts['parallax_type'] ) {
			$return .= ' background-size: cover; ';
		}

		$return .= ( '' != $sc_atts['style']) ? esc_attr($sc_atts['style']) : ''  ;

		$return .= '"';
	}

	// Parallax dynamic
	if ( 'yes' == $sc_atts['pattern'] && 'dynamic' == $sc_atts['parallax_type'] ) {
		$return .= ' data-amount="50%"' ;

		$return .= ' data-scroll="' ;
		$return .= ('' != $sc_atts['parallax_scroll']) ? '' . esc_attr($sc_atts['parallax_scroll']) : '0.5' ;
		$return .= '"' ;

		$return .= ' data-duration="' ;
		$return .= ('' != $sc_atts['parallax_duration']) ? '' . esc_attr($sc_atts['parallax_duration']) : '100' ;
		$return .= '"' ;

		$return .= ' data-easing="' ;
		$return .= ('' != $sc_atts['parallax_easing']) ? '' . esc_attr($sc_atts['parallax_easing']) : '' ;
		$return .= '"' ;
	}



    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '><div class="row"><div class="grid12">';

    $return .= do_shortcode( $content ) . '</div></div></div>';

    return $return ;
}
}

// TABLE
if ( !function_exists('ishyoboy_shortcode_table') ) {
function ishyoboy_shortcode_table($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'striped' => 'no',
        'background' => 'no'
    ), $atts);

    $return = '';

    $return .= '<div class="table-content';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' table-center' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' table-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' table-left' : '' ;
    $return .= ('yes' == $sc_atts['striped']) ? ' table-striped' : '' ;
    $return .= ('yes' == $sc_atts['background']) ? ' table-bg' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// PRICING TABLE
if ( !function_exists('ishyoboy_shortcode_pricing_table') ) {
function ishyoboy_shortcode_pricing_table($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'striped' => 'no',
        'background' => 'no'
    ), $atts);

    $return = '';

    $return .= '<table class="table-content';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' table-center' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' table-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' table-left' : '' ;
    $return .= ('yes' == $sc_atts['striped']) ? ' table-striped' : '' ;
    $return .= ('yes' == $sc_atts['background']) ? ' table-bg' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</table>';

    return $return ;
}
}

// PRICING ROW
if ( !function_exists('ishyoboy_shortcode_pricing_row') ) {
function ishyoboy_shortcode_pricing_row($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => 'color1',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'headline' => 'no',
        'highlight' => 'no'
    ), $atts);

    $return = '';

    $return .= '<tr>';
    $return .= ('yes' == $sc_atts['headline']) ? '<th class="': '<td class="' ;

    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' table-center' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' table-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' table-left' : '' ;
    $return .= ('yes' == $sc_atts['highlight']) ? ' highlight-col' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content );
    $return .= ('yes' == $sc_atts['headline']) ? '</th>': '</td>' ;
    $return .= '</tr>';

    return $return ;
}
}

// CHART
if ( !function_exists('ishyoboy_shortcode_chart') ) {
function ishyoboy_shortcode_chart($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'percent' => '0',
        'icon' => '',
        'line_width' => '',
        'rounded' => 'no',
        'size' => '',
        'bar_color' => '',
        'track_color' => '',
        'text_size' => '',
        'animation_time' => ''
    ), $atts);

    global $ish_options;

    wp_enqueue_script( 'easy_pie_chart' );

    $return = '';

    switch ($sc_atts['color']){
        case 'color1' :
            if ('' == $sc_atts['bar_color']) { $sc_atts['bar_color'] = $ish_options[$sc_atts['color']]; }
            if ('' == $sc_atts['track_color']) { $sc_atts['track_color'] = $ish_options['color3']; }
            break;
        case 'color2' :
            if ('' == $sc_atts['bar_color']) { $sc_atts['bar_color'] = $ish_options[$sc_atts['color']]; }
            if ('' == $sc_atts['track_color']) { $sc_atts['track_color'] = $ish_options['color3']; }
            break;
        case 'color3' :
            if ('' == $sc_atts['bar_color']) { $sc_atts['bar_color'] = $ish_options[$sc_atts['color']]; }
            if ('' == $sc_atts['track_color']) { $sc_atts['track_color'] = $ish_options['color2']; }
            break;
        case 'color4' :
            if ('' == $sc_atts['bar_color']) { $sc_atts['bar_color'] = $ish_options[$sc_atts['color']]; }
            if ('' == $sc_atts['track_color']) { $sc_atts['track_color'] = $ish_options['color3']; }
            break;
        default :
            if ('' == $sc_atts['bar_color']) { $sc_atts['bar_color'] = $ish_options['color1']; }
            if ('' == $sc_atts['track_color']) { $sc_atts['track_color'] = $ish_options['color3']; }
    }

    $sc_atts['bar_color'] = ishyoboy_colors_to_hex($sc_atts['bar_color']);
    $sc_atts['track_color'] = ishyoboy_colors_to_hex($sc_atts['track_color']);

    $return .= '<div class="chart';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['icon']) ? ' ' . esc_attr($sc_atts['icon']) : '' ;
    $return .= ('center' == $sc_atts['align']) ? ' chart-center' : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' chart-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' chart-left' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= ('' != $sc_atts['percent']) ? ' data-percent="' . esc_attr($sc_atts['percent']) . '"' : ''  ;
    $return .= ('' != $sc_atts['line_width']) ? ' data-linewidth="' . esc_attr($sc_atts['line_width']) . '"' : ''  ;
    $return .= ('yes' == $sc_atts['rounded']) ? ' data-linecap="round"' : ''  ;
    $return .= ('' != $sc_atts['size']) ? ' data-size="' . esc_attr($sc_atts['size']) . '"' : ''  ;
    $return .= ('' != $sc_atts['bar_color']) ? ' data-barcolor="' . esc_attr($sc_atts['bar_color']) . '"' : ''  ;
    $return .= ('' != $sc_atts['track_color']) ? ' data-trackcolor="' . esc_attr($sc_atts['track_color']) . '"' : ''  ;
    $return .= ('' != $sc_atts['animation_time']) ? ' data-animate="' . esc_attr($sc_atts['animation_time']) . '"' : ''  ;

    if ('' != $sc_atts['style'] || '' != $sc_atts['text_size']){
        $return .= ' style="';

        $return .= ('' != $sc_atts['text_size']) ? ' font-size: ' . esc_attr($sc_atts['text_size']) . 'px;' : '';
        $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';

        $return .= '"';

    }

    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// TIMELINE
if ( !function_exists('ishyoboy_shortcode_timeline') ) {
function ishyoboy_shortcode_timeline($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'hover_effect' => 'no'
    ), $atts);

    $return = '';

    $return .= '<div class="timeline';

    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('yes' == $sc_atts['hover_effect']) ? ' timeline-hover' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// TIMELINE
if ( !function_exists('ishyoboy_shortcode_timeline_item') ) {
function ishyoboy_shortcode_timeline_item($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'size' => 'small' // point-big, point-medium, point-small,
    ), $atts);

    $return = '';

    $return .= '<div class="point-';
    $return .= ('' != $sc_atts['size']) ? esc_attr($sc_atts['size']) : 'small' ;
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $return .= do_shortcode( $content ) . '</div>';

    return $return ;
}
}

// TIMELINE DATE
if ( !function_exists('ishyoboy_shortcode_timeline_date') ) {
function ishyoboy_shortcode_timeline_date($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'border' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="timeline-date';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' timeline-' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['border']) ? ' timeline-border' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '><div><div>';

    $return .= do_shortcode( $content ) . '</div></div></div>';

    return $return ;
}
}

// TIMELINE CONTENT
if ( !function_exists('ishyoboy_shortcode_timeline_content') ) {
function ishyoboy_shortcode_timeline_content($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'border' => ''
    ), $atts);

    $return = '';

    $return .= '<div class="timeline-content';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' timeline-' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('yes' == $sc_atts['border']) ? ' timeline-border' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '><div><div>';

    $return .= do_shortcode( $content ) . '</div></div></div>';

    return $return ;
}
}

// ROUNDED IMAGE
if ( !function_exists('ishyoboy_shortcode_rounded_image') ) {
function ishyoboy_shortcode_rounded_image($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'arrow' => '',
        'width' => '',
        'border_width' => ''
    ), $atts);

    $return = '';
    $return .= '<div class="rounded-image ri-border';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('' != $sc_atts['arrow']) ? ' ri-arrow ri-arrow-' . esc_attr($sc_atts['arrow']) : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' ri-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' ri-left' : '' ;
    //$return .= ('center' == $sc_atts['align']) ? ' ri-center' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    if ('' != $sc_atts['style'] || '' != $sc_atts['width'] ){
        $return .= ' style="';

        $return .= ('' != $sc_atts['width']) ? ' max-width: ' . esc_attr($sc_atts['width']) . 'px;' : '';
        $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';

        $return .= '"';

    }
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '><div';

    if ('' != $sc_atts['border_width'] ){
        $return .= ' style="';

        $return .= ('' != $sc_atts['border_width']) ? ' border-width: ' . esc_attr($sc_atts['border_width']) . 'px;' : '';

        $return .= '"';
    }

    $return .= '>';

    $return .= do_shortcode( $content ) . '</div></div>';

    return $return ;
}
}

// FEATURED IMAGE
if ( !function_exists('ishyoboy_shortcode_featured_image') ) {
function ishyoboy_shortcode_featured_image($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'url' => '',
        'show_caption' => 'yes',
        'new_window' => 'no',
        'open_full_version' => 'no',
        'size' => 'theme-large'
    ), $atts);

    $return = '';

    if ( (function_exists('has_post_thumbnail')) && (has_post_thumbnail()) ) {
        $return .= '<div class="featured-image';
        $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
        $return .= ('right' == $sc_atts['align']) ? ' right' : '' ;
        $return .= ('left' == $sc_atts['align']) ? ' left' : '' ;
        $return .= ('center' == $sc_atts['align']) ? ' center' : '' ;
        $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
        $return .= '"' ;
        $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
        $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
        $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
        $return .= '>';

        if ( '' != $sc_atts['url'] || 'yes' != $sc_atts['open_full_version'] ){

            if ( 'yes' == $sc_atts['open_full_version'] ){
                $large_image_url = wp_get_attachment_image_src( get_post_thumbnail_id(get_the_ID()), 'full');
                $return .= '<a href="' . esc_attr( $large_image_url[0] ) . '" alt=""' . ( ( 'yes' == esc_attr( $sc_atts['new_window'] ) ) ? ' target="_blank"' : '' ) . '>';

            } else{

                $return .= '<a href="' . esc_attr( apply_filters( 'ishyoboy_sc_url', $sc_atts['url'] ) ) . '" alt=""' . ( ( 'yes' == esc_attr( $sc_atts['new_window'] ) ) ? ' target="_blank"' : '' ) . '>';

            }

        }

        $return .= get_the_post_thumbnail( get_the_ID(), $sc_atts['size']) ;

        $return .= ( '' != $sc_atts['url'] || 'yes' != $sc_atts['open_full_version'] ) ? '</a>' : '' ;
        $return .= ('yes' == $sc_atts['show_caption']) ? '<div class="wp-caption"><p class="wp-caption-text">' . get_the_post_thumbnail_caption() . '</p></div>' : '';

        $return .= '</div>';
    }

    return $return ;
}
}

// NAVIGATION
if ( !function_exists('ishyoboy_shortcode_navigation') ) {
function ishyoboy_shortcode_navigation($atts, $content = null){
    $sc_atts = shortcode_atts( array(
        'id' => '',
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'menu' => '',
        'depth' => '0'
    ), $atts);

    $return = '';

    $return .= '<div class="sc-nav-menu';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('' != $sc_atts['color']) ? ' ' . esc_attr($sc_atts['color']) : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' ri-right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' ri-left' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';
    $return .= ('' != $sc_atts['style']) ? ' style="' . esc_attr($sc_atts['style']). '"' : ''  ;
    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;
    $return .= '>';

    $menu = wp_nav_menu(
        array(
            'menu' => $sc_atts['menu'],
            'depth' => $sc_atts['depth'],
            'echo' => false
        )
    );

    $return .= str_replace(array("\r\n", "\n", "\r"), '', $menu);

    $return .= '</div>';

    return $return ;
}
}

$map_count = 0;

// MAP
if ( !function_exists('ishyoboy_shortcode_map') ) {
function ishyoboy_shortcode_map($atts, $content=null){
    global $map_count, $ish_options;
    $map_count++;

    $sc_atts = shortcode_atts( array(
        'id' => 'google-map-' . $map_count,
        'css_class' => '',
        'style' => '',
        'color' => '',
        'tooltip' => '',
        'tooltip_color' => '',
        'align' => '',
        'lat_lng_1' => '',
        'lat_lng_2' => '',
        'lat_lng_3' => '',
        'lat_lng_4' => '',
        'lat_lng_5' => '',
        'lat_lng_6' => '',
        'lat_lng_7' => '',
        'lat_lng_8' => '',
        'lat_lng_9' => '',
        'lat_lng_10' => '', // Supports up to 10 addresses
        'zoom' => '15',
        'invert_colors' => 'no',
        'height' => ''
    ), $atts);


    if ( isset( $ish_options[ $sc_atts['color'] ] ) ){
        $sc_atts['color'] = $ish_options[ $sc_atts['color'] ];
    }

    $return = '';

    $return .= '<div class="google-map';
    $return .= ('' != $sc_atts['css_class']) ? ' ' . esc_attr($sc_atts['css_class']) : '' ;
    $return .= ('right' == $sc_atts['align']) ? ' right' : '' ;
    $return .= ('left' == $sc_atts['align']) ? ' left' : '' ;
    $return .= ('' != $sc_atts['tooltip'] && '' != $sc_atts['tooltip_color']) ? ' tooltip-' . esc_attr($sc_atts['tooltip_color']) : ''  ;
    $return .= '"' ;
    $return .= ('' != $sc_atts['id']) ? ' id="' . esc_attr($sc_atts['id']) . '"' : '';

    if ('' != $sc_atts['style'] || '' != $sc_atts['height']){
        $return .= ' style="';

        $return .= ('' != $sc_atts['height']) ? ' height: ' . esc_attr($sc_atts['height']) . 'px;' : '';
        $return .= ('' != $sc_atts['style']) ? ' ' . esc_attr($sc_atts['style']) : '';

        $return .= '"';

    }

    $return .= ('' != $sc_atts['tooltip']) ? ' data-type="tooltip" title="' . esc_attr($sc_atts['tooltip']) . '"' : ''  ;

    $i = 1;
    while ( isset( $sc_atts['lat_lng_' . $i]) && '' != $sc_atts['lat_lng_' . $i] ){
        $return .= ' data-latlng' . $i . '="' . esc_attr( $sc_atts['lat_lng_' . $i] ) . '"';
        $i++;
    }

    $return .= ('' != $sc_atts['zoom']) ? ' data-zoom="' . esc_attr($sc_atts['zoom']) . '"' : ''  ;
    $return .= ('yes' == $sc_atts['invert_colors']) ? ' data-invert="' . esc_attr($sc_atts['invert_colors']) . '"' : ''  ;
    $return .= ('' != $sc_atts['color']) ? ' data-color="' . esc_attr($sc_atts['color']) . '"' : '' ;
    $return .= '></div>';

    return $return;
}
}


//Register all shortcodes
foreach ( $ishyoboy_shortcodes as $tag => $func ){
    add_shortcode( $tag, $func );
}

// Actual processing of the shortcode happens here

if ( !function_exists('ishyoboy_do_shortcode') ) {
	function ishyoboy_do_shortcode( $content ) {
	    global $shortcode_tags, $ishyoboy_shortcodes;

	    // Backup current registered shortcodes and clear them all out
	    $orig_shortcode_tags = $shortcode_tags;
	    remove_all_shortcodes();

	    //Register only IshYoBoy shortcodes
	    $shortcode_tags = $ishyoboy_shortcodes;

	    // Do all ishyoboy shortcodes above
	    $content = do_shortcode( $content );

	    // Restore the original shortcodes back
	    $shortcode_tags = $orig_shortcode_tags;

	    return $content;
	}
}
add_filter( 'the_content', 'ishyoboy_do_shortcode', 7 );


/**
 * Register all shortcodes into TinyMCE
 */
if ( !function_exists('ishyoboy_register_tinymce_buttons') ) {
	function ishyoboy_register_tinymce_buttons() {
		if ( current_user_can('edit_posts') || current_user_can('edit_pages') )
		{
			add_filter('mce_external_plugins', 'ishyoboy_add_plugin');
			add_filter('mce_buttons', 'ishyoboy_register_buttons');
		}
	}
}

if ( !function_exists('ishyoboy_register_buttons') ) {
	function ishyoboy_register_buttons($buttons) {
		array_push($buttons, "ishyoboy_columns_shortcodes");
		array_push($buttons, "ishyoboy_typography_shortcodes");
		array_push($buttons, "ishyoboy_block_shortcodes");
		array_push($buttons, "ishyoboy_special_shortcodes");
		return $buttons;
	}
}

if ( !function_exists('ishyoboy_add_plugin') ) {
	function ishyoboy_add_plugin($plugin_array) {
		global $tinymce_version;

		if ( version_compare( $tinymce_version, '4000' ) >= 0 ) {
			$plugin_array['ishyoboy_shortcodes_select'] = IYB_FRAMEWORK_URI . '/libs/js/shortcodes_4.js';
		} else {
			$plugin_array['ishyoboy_shortcodes_select'] = IYB_FRAMEWORK_URI . '/libs/js/shortcodes.js';
		}

		return $plugin_array;
	}
}

add_action('init', 'ishyoboy_register_tinymce_buttons');


/*
 *  Adds a custom stylesheet to the tinymce editor.
 */
if ( !function_exists('ishyoboy_register_tinymce_editor_styles') ) {
	function ishyoboy_register_tinymce_editor_styles( $url ) {

		if ( ! empty($url) )
			$url .= ',';

		// Change the path here if using sub-directory
		global $tinymce_version;

		if ( version_compare( $tinymce_version, '4000' ) >= 0 ) {
			$url .= IYB_FRAMEWORK_URI . '/libs/css/tinymce_4.css';
		} else {
			$url .= IYB_FRAMEWORK_URI . '/libs/css/tinymce.css';
		}

		return $url;

	}
}

if ( !function_exists('ishyoboy_register_admin_tinymce_styles') ) {
	function ishyoboy_register_admin_tinymce_styles() {
		/* Register our stylesheet. */
		global $tinymce_version;

		if ( version_compare( $tinymce_version, '4000' ) >= 0 ) {
			wp_register_style( 'myPluginStylesheet', IYB_FRAMEWORK_URI . '/libs/css/tinymce_4.css' );
		} else {
			wp_register_style( 'myPluginStylesheet', IYB_FRAMEWORK_URI . '/libs/css/tinymce.css' );
		}

		wp_enqueue_style( 'myPluginStylesheet' );
	}
}

add_action( 'admin_init', 'ishyoboy_register_admin_tinymce_styles' );
add_filter( 'mce_css', 'ishyoboy_register_tinymce_editor_styles' );